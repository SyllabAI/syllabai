# TODO.md — SyllabAI Master Work Queue (Cycle 1 first)

**Scope guard:** only rows marked `Cycle 1` in `backlog/syllabai-master-project.xlsx` belong in the current execution window. The 12-spine critical path is F-020 → F-032 → F-033 → F-040 → F-041/F-043 → F-047 → F-055 → F-137/F-138 → F-148 → F-160.

## Wave 0 — foundations (this week)

- [x] T-001 Bootstrap `syllabai-core`: Spring Boot 4.1.x + Java 25 + Spring AI 2.0 skeleton, Maven, package structure per Master Spec §30. (F-002, F-008)
- [x] T-002 Flyway + Neon connection; pgvector extension enabled; dev profile with local Postgres via Docker Compose.
- [x] T-003 Spring Security + JWT auth skeleton; RBAC roles (STUDENT, TEACHER, ADMIN). (F-012, F-013)
- [x] T-004 REST conventions: `/api/v1` base, DTO boundaries, springdoc OpenAPI, error handling. (F-122)
- [x] T-005 `syllabai-web`: Next.js 16 + TypeScript + Tailwind scaffold on Vercel; API client typed from OpenAPI.
- [x] T-006 CI: GitHub Actions (build + test) on `syllabai-core` and `syllabai-web`.
- [x] T-007 Object storage adapter + Cloudflare R2 free bucket for past-paper PDFs. (F-021, F-149)

## Wave 1 — knowledge & content fabric

- [x] T-008 Canonical document format schema (JSON) + `DocumentParser` contract. (F-134, F-147) — syllabai-parser @ a43ad24, schema 1.0, 23/23 tests
- [x] T-009 In-process opendataloader-pdf integration (Maven) for text PDFs; bounding boxes retained. — opendataloader-pdf-core 2.5.7 (Apache-2.0, Central) 
- [x] T-010 Syllabus ingestion: Edexcel IAL Chemistry spec → curriculum tables → KG seed. (F-020, F-032) — parser `edexcel-numbered-outline-v1` (deterministic, per-node §17 provenance; real Pearson 2018 spec: 6 units / 20 topics / 15 subtopics) + core `CurriculumIngestionService` (idempotent, all-SUGGESTED) + teacher validation gate; prerequisites stay teacher-curated (never derived from an outline)
- [x] T-011 Past-paper + mark-scheme ingestion bridge: parser draft JSON → all-SUGGESTED content bank (paper/version/parts/scheme/points), teacher validation workflow, ingestion-anchor KG topic. Live-verified on real 4CH0/1C Jan 2012 (27 q / 62 parts / 33 points). Misconception/distractor links on structured items: Wave 3 (needs validated content). (F-028, F-029, F-152)
- [x] T-012 `KnowledgeGraphRepository` with recursive-CTE traversals; prerequisite queries. (F-032, F-135)
- [x] T-013 pgvector embedding pipeline (Gemini embeddings) over mark schemes/notes. (F-039, F-136) — core @ V11: canonical document store + deterministic chunking + Gemini text-embedding-004 (768-dim, `EmbeddingProvider` port, no failover by design) + HNSW cosine search; 121 unit tests + ContentPipelineIT

## Wave 2 — learner & assessment engine

- [x] T-014 `AssessmentEvidence` contract + attempt logging with timing, confidence, doubt flags. (F-053, F-055, F-056, F-153)
- [x] T-015 BKT engine with configurable params (L0=0.1, slip=0.1, guess=0.25, T=0.1) + parameter registry. (F-138)
- [x] T-016 Learner state aggregate: mastery, misconceptions, fluency (timed-vs-untimed gap, V8), confidence + self-doubt evidence. (F-137, F-033) — exam-literacy aggregate deferred to Wave 4 (needs marked structured data first)
- [x] T-017 BDT misconception engine driven by distractor evidence. (F-140)
- [x] T-018 Ebbinghaus decay nightly job (τ 30/90/365) + review-schedule feed. (F-159)
- [x] T-019 Timed vs untimed paired conditions + procedural_fluency_gap (V8 skill_states column, recomputed per node on evidence; null until paired). (F-162)
- [x] T-020 Learning-log telemetry: Paper B §3.5 fields, append-only event store. (F-160)
- [x] T-021 Smart Mark pipeline: candidate generation (LLM, pinned prompt v1) → deterministic bounds/coverage/mark-sum validation → append-only results; blank answers deterministic (no LLM). LLM never final truth. (F-047)
- [x] T-022 Human mark override + κ agreement gate: per-point paired Cohen's κ, threshold 0.60 recorded per evaluation, fail-closed gate; pre-gate smart marks provisional, post-gate authoritative; overrides never re-fire BKT. (F-144, F-161)

## Wave 3 — tutor & diagnostic intelligence

- [x] T-023 `LlmProvider` + free chain (Groq → Gemini → OpenRouter) with failover and rate tracking. (F-148, F-163)
- [x] T-024 KA-RAG orchestration: intent → KG context → hybrid retrieval → grounded generation. (F-040) — v0 foundation: deterministic intent (VALIDATED nodes only), KG+vector RRF fusion (rank-only, k=60), NoReranker Strategy, learner-state context assembly, grounded generation (prompt tutor-grounded/v1, free-LLM chain), citations with deep links, KA_RAG_COMPLETED telemetry (V12), deterministic refusal on empty evidence; POST /api/v1/tutor/ask (backend surface — chat UI stays T-025)
- [x] T-025 Tutor chat UI with verbatim citations + PDF deep-links. (F-041, F-043, F-022) — **COMPLETE and MERGED 2026-09-07 (explicit user instruction): web PR #1 merged as a fast-forward push (main `1685f4c` → `3402230`, no merge commit — linear history, main is exactly the CI-verified head; PR closed/merged, merge_commit_sha = `3402230`); post-merge CI run 34049500636 on main — Lint + type-checked production build both SUCCESS (step-level verified, not assumed).** Implementation (session 13, branch `codex/t-025-tutor-chat` @ `3402230`, PR CI run 34047801205 SUCCESS): TutorChatView — a new Tutor tab on the merged T-024 surface `POST /api/v1/tutor/ask` (synchronous v0, no SSE backend exists — loading/error/refusal states, no second backend architecture invented); `[n]` markers render as reference chips jumping to verbatim-source citation cards; matched-topic chips; refusal state distinct; traceability footer (model · provider · latency · evidence count, Spec §19); transcript survives tab switches; role-aware deep links (documented v0 decision); 2000-char limit mirroring backend `@Size`. Follow-ups that remain open on F-041/F-043 (tracked there, not blocking): browser verification of the chat flow against the live backend at pilot time; SSE/streaming when the backend ships it (Spec §22 future surface); in-app PDF deep-links stay F-022 (deliberately not started). T-026/T-027 diagnosis-aware policy integration still gated on unmerged core PR #1 (human decision, never automatic).
- [x] T-026 Tutor policy: diagnosis-aware intervention selection (Scientific-learning-skills prompts). — **COMPLETE and MERGED 2026-09-08 (explicit user directive this session: "merge/verify the existing work, finish the end-to-end adaptive student experience"): core PR #1 was rebased onto current main (T-033 already in) and its `V13__diagnosis_tutor_policy.sql` renumbered to **V14** — the old V13 collided with the already-merged `V13__glm_ocr_bridge_records.sql` (git-mergeable but Flyway would fail with duplicate version 13); no SQL content changed. Verification: 264/264 unit green locally on `04d6f10` (228 from main + 36 diagnosis tests); CI run 34151840343 SUCCESS on the rebased head incl. Docker ITs (Flyway V14 applies on a fresh schema); post-merge CI run 34152066031 on main SUCCESS. PR #1 marked ready (draft flag cleared via GraphQL) before the fast-forward merge; verification + authorization evidence posted to the PR. Main: `18e92ee` → `04d6f10`.** v0.2 semantics unchanged from the audited branch: precedence documented, teacher-override reads, struggle-type → intervention mapping with explainability (ADR-016 lineage).
- [x] T-027 Struggle inference v0: rule-based signals over learning log. (F-141) — **COMPLETE and MERGED with T-026 (core PR #1, see above): prereq-gap / 3b fluency / 5a self-doubt; supersede-keeps-history, 7-day expiry enforced on reads; unsupported types never emitted, tested.**
- [x] T-033 Next Best Learning Action v1 — the adaptive-loop closing surface (F-092 minimal Cycle-1 slice, promoted by the 2026-09-08 finish-the-product directive; ADR-017 deterministic baseline). — **COMPLETE and MERGED 2026-09-08 (explicit user directive: "merge/verify the existing work…"): the documented merge order was respected — core PR #8 FIRST (main `c7d22b9` → `18e92ee`, post-merge CI run 34151756702 SUCCESS), then web PR #4 (main `2c404a4` → `9e2931d`, post-merge CI run 34151763333 SUCCESS). Local re-verification before merging: core 228/228 unit on `18e92ee`; web lint clean + type-checked production build on `9e2931d`.** Backend: `recommendation` module — `NextBestActionService` (deterministic `nba-rules/v1`: rule tiers DUE_REVIEW → PREREQUISITE_WEAK → PROBLEM_QUESTION → MISCONCEPTION_SUSPECTED → FLUENCY_GAP → LOW_MASTERY → UNCOVERED_TOPIC with structured evidence-backed reason codes; one action per topic; capped portfolio; subject-subtree hard isolation; only servable question references) on `GET /api/v1/learners/me/recommendations?rootId=` (Spec §22); `ServableQuestionService` extracted as the single owner of the learner-serving boundary (QuestionController delegates, behavior identical — guarded by the existing ITs). Web: `NextBestActionsCard` on the dashboard (ranked advice with the backend's verbatim reasonDetail, deep-links to practice/tutor, honest empty/error states, distinct from the measured-fact cards). Scope guard: the full F-087…F-093 architecture (learned ranking, collaborative filtering, exploration machinery, exposure evaluation) stays Cycle 2+ — only this minimal slice is Cycle 1.
- [x] T-034 Attempt history + tutor handoff — the Review Hub minimal slice (F-059 attempt-history leg, charter §14; closes the missing loop leg of the end-to-end adaptive student experience). — **COMPLETE and MERGED 2026-09-08 (same session directive): core PR #9 (head `b71703b`, CI run 34152799603 SUCCESS incl. the new AttemptHistoryFlowIT with real Postgres; local 270/270 unit) → web PR #5 (head `99194af`, CI run 34152809194 SUCCESS — lint + type-checked production build). Merged fast-forward in dependency order: core main `04d6f10` → `b71703b`, then web main `9e2931d` → `99194af`; merge evidence posted to both PRs.** Backend: `AttemptHistoryService` + `AttemptHistoryController` — `GET /api/v1/learners/me/attempts?limit=` (default 50, max 100; identity from the JWT): strictly a read-only view over the immutable attempts/answers evidence rows — NO parallel tracking, NO derived mastery; honesty rules pinned by tests (structured attempts → null marks while any part is pending; attempt-level marks = sum of authoritative part marks only; MCQ rows carry the already-revealed chosen/correct option labels + the distractor misconception; evidenceEmitted rides along; topic identity via the shared KG, null-safe). Web: new **History** tab (`HistoryView`) — outcome/marks/marking-state/topic/timing/calibration per attempt, "Practise topic" deep-link closing evidence → review → retry, honest empty/error/awaiting-marks states; **Ask tutor about this** on a wrong MCQ result pre-fills an editable tutor question (never auto-sends) closing the weakness → tutor leg; history refreshes after every submission. Deliberately out of scope (stays F-059 Cycle 2+): problematic-questions aggregation, filters, due-review cross-linking — only the attempt-history leg is Cycle 1.
- [x] T-028 Mastery map UI (KG visualizer, simplified 2D first) + student dashboard. (F-036, F-060, F-034) — **COMPLETE and MERGED 2026-09-07 (session 15, explicit user authorization "OKAY PROCEED" on the session-14 pending merge decisions). The documented merge order was respected: core PR #6 FIRST, then web PR #2 — both merged as fast-forward pushes (linear history, no merge commit; each main is exactly the CI-verified head, GitHub marked both PRs closed/merged with merge_commit_sha = the pushed head). Pre-merge verification (API, not worklog): both PRs open + mergeable_state clean on the documented heads/bases, PR CI runs 34050895468 (core) and 34051246049 (web) completed/success; core PR #1 (T-026/T-027) verified open/draft/unmerged before AND after — the authorization touched only PR #6 and PR #2. Post-merge verification (observed, step-level): core main `1976540` → `ab7a52b`, push CI run 34053354490 on main — BUILD SUCCESS (Build and test, Upload test reports steps green; 204/204 unit + 22/22 IT incl. GlmOcrBatchIT 5/5 per the PR-run log inspection); web main `3402230` → `18543d2`, push CI run 34053451121 on main — Lint ✓, Production build (type-checked) ✓. Follow-ups that remain open (tracked on F-034/F-036/F-060, not blocking): browser verification of the dashboard/graph flows against the live backend at pilot time (same deferred-verification posture as T-025); the deeper F-036 polish (pan/zoom, 3D) stays out of Cycle-1 scope. Implementation history (session 14, branches):**
  - **Core PR #6 — MERGED** (fast-forward `1976540` → `ab7a52b`; PR closed/merged, merge_commit_sha `ab7a52b`; post-merge CI 34053354490 SUCCESS): F-034 `GET /api/v1/learners/me/knowledge-graph?rootId=...` (Spec §22) — `LearnerKnowledgeGraphService` composes the VERIFIED read services (`KnowledgeGraphService.treeWithMisconceptions` + BKT skill states with real `EbbinghausDecayService` decay + BDT misconception states + earliest pending review per node; no new persistence/traversal/mutation); `KnowledgeGraphService.prerequisiteRelations` (one batched `findPrerequisiteEdgesWithin` query over the recursive-CTE subtree); `DecayParams.bandOf` (band vocabulary beside its thresholds — `LearnerStateController` delegates, so /state and /knowledge-graph can never disagree). Honesty rules pinned by tests: unpractised nodes render nulls (never zeros/fabricated bands); misconception annotations only on MISCONCEPTION nodes; reviewDueAt = earliest PENDING review; `asOf` pins the decay instant. Local `mvn test` (JDK 25.0.4.1 + Maven 3.9.9, tools staged in the workspace): **204/204** (196 existing + 8 new). **PR CI run 34050895468 SUCCESS — step-level verified (mvn verify: 204/204 unit + 22/22 IT incl. GlmOcrBatchIT 5/5 regression proofs + KaRagFlowIT 3/3; log-downloaded and inspected, 0 failures/errors/skipped, BUILD SUCCESS).**
  - **Web PR #2 — MERGED** (fast-forward `3402230` → `18543d2`, after core PR #6 per the runtime dependency; PR closed/merged, merge_commit_sha `18543d2`; post-merge CI 34053451121 SUCCESS — Lint ✓, Production build type-checked ✓): `DashboardView` (F-060) — mastery summary (band chips + avg effective mastery + as-of), due reviews (overdue first + Practise CTA), focus areas (lowest MEASURED mastery — labeled facts, not recommendations), misconception watch, recent activity, predicted grade = explicit not-implemented state (never a fabricated number); `KnowledgeGraphView` (F-036) — 2D-first SVG (layered columns by KG depth, band-colored nodes, solid PART_OF + dashed prerequisite edges with arrowheads, legend, keyboard-selectable nodes with aria-labels; tree view stays the accessible alternative); `MasteryMap` refactored onto ONE personalized payload — the client-side tree + /state join is retired (F-034's purpose); Tree/Graph toggle + graph node detail card + on-demand prerequisite chains + Practise CTAs; controlled tabs with dashboard → practice topic deep links (`api.questions(topicNodeId)` already existed; honest "no validated questions on this topic yet" empty state); `lib/format.ts` shared formatters (StateView deduped). Local `bun run lint` clean + `bunx next build` (type-checked) SUCCESS. **PR CI run 34051246049 SUCCESS — step-level verified (Lint ✓, Production build type-checked ✓).**
  - Deliberately out of scope: 3D force-graph (F-036 is 2D-first by scope), pan/zoom, SSE, teacher surfaces (T-029 next), recommendation policy (the recommendation package stays empty), any change to GLM-OCR/T-C04 state, core PR #1 (T-026/T-027) untouched.

## Wave 4 — pilot hardening

- [x] T-029 Teacher minimal surface: class list, Smart Mark review queue, overrides. — **COMPLETE and MERGED 2026-09-07 (session 16, after the user-mandated pre-merge audit passed). The documented merge order was respected: core PR #7 FIRST, then web PR #3 — both merged as fast-forward pushes (linear history, no merge commit; each main is exactly the CI-verified head, GitHub marked both PRs closed/merged with merge_commit_sha = the pushed head). The audit added one test-only commit to the core branch BEFORE merging (real-HTTP JWT RBAC regression `TeacherRouteSecurityIT`); no product code changed by the audit.**
  - **Core PR #7 — MERGED** (fast-forward `ab7a52b` → `c7d22b9`; PR closed/merged, merge_commit_sha `c7d22b9`; branch CI 34054418641 on `095a9c1` SUCCESS (214/214 unit + 23/23 IT) and audit-fix head CI **34093970676** on `c7d22b9` SUCCESS — 240/240 tests: 217 unit (214 executed + 3 new security IT) + 23/23 IT; post-merge main CI **34094254832** SUCCESS, log-inspected: 0 failures/errors/skipped, TeacherRouteSecurityIT 3/3, GlmOcrBatchIT 5/5, GlmOcrBridgeIT 7/7, MultipartMarkingFlowIT 2/2, KaRagFlowIT 3/3, BUILD SUCCESS): `GET /api/v1/teacher/learners` (`TeacherRosterController` + `UserRepository.findEnabledByRole` JPQL — the honest class list IS the enabled STUDENT cohort; the pilot has no class entity; identity projection only: id/displayName/email/createdAt, test-pinned to exactly 4 record components so password hashes/roles cannot leak); `learnerDisplayName` composed SERVER-SIDE into `AnswerMarkingView` (one batched `findAllById` per queue read; empty queue skips the lookup — the F-034 lesson: server composes the teacher read model, no client-side join). The marking/override/κ backend itself already existed on main (T-021/T-022). **Authorization (audit section 4, the mandatory proof): backend route rule `SecurityConfig` `.requestMatchers("/api/v1/teacher/**").hasAnyRole("TEACHER","ADMIN")` — NOT frontend hiding; now regression-proven by `TeacherRouteSecurityIT` (real HTTP + real JWTs over the embedded server: a STUDENT token is proven VALID on its own `/api/v1/learners/me/state` surface then denied 403 on ALL seven teacher endpoints incl. roster/queue/detail/smart-mark/human-mark/κ-latest/κ-evaluate; anonymous and forged tokens 401 with no email leakage in the body; TEACHER and ADMIN 200).** One CI iteration pre-audit, a real find: the new IT ingested the same draft as the sibling test → duplicate `uq_knowledge_node_code` on the ingestion anchor → drafts now take distinct paper codes per test.
  - **Web PR #3 — MERGED** (fast-forward `18543d2` → `2c404a4`, after core PR #7 per the runtime dependency; PR closed/merged, merge_commit_sha `2c404a4`; post-merge main CI **34094459463** SUCCESS — Lint ✓, Production build type-checked ✓, log-inspected "Compiled successfully" + "Running TypeScript"): role-gated Teacher tab (TEACHER/ADMIN — a UI affordance, never authorization; the backend route rule above is the boundary); `TeacherReviewView` — class list (cohort table with honest "classes are a Cycle-2+ concept" framing), marking queue with state tabs mapping exactly the backend `Answer.MarkingState` values (PENDING/SMART_MARKED/HUMAN_MARKED/OVERRIDDEN), answer review panel (part prompt, learner answer, latest Smart Mark with failure reasons rendered verbatim — never fabricated — and model/pipeline traceability, latest human mark), **Run Smart Mark** action (single-flight, refreshes detail+queue+κ), **Record human mark (override)** form (0..partMarks integer validation client-side, backend re-validates via ConflictException; per-point decisions seeded from the smart breakdown for κ pairing, rationale comments; first-mark-fires-evidence-once / overrides-never-re-fire semantics in the UI copy), κ gate panel (latest evaluation κ/sample/threshold/passed/scope/age + recompute + honest 404 "no evaluation recorded yet" / 409 "no paired decisions available yet" states — fail-closed framing). Local `bun run lint` clean + `bunx next build` (type-checked) SUCCESS (audit-rerun on the branch head). API integration audited: all 7 client methods match backend paths/DTOs exactly, no hardcoded learner or marking IDs anywhere.
  - Deliberately out of scope: class management (no class entity in Cycle 1), teacher analytics/heatmaps (F-074 is Cycle 2+), content-validation workflow UI (T-C04 acceptance criteria unchanged — this surface must not be declared to satisfy T-C04), per-learner diagnostics drill-downs (§6.10 deeper items), SSE. Note: this UI may eventually provide an application-native surface for portions of the T-C04 human validation workflow, but that does NOT redefine T-C04, and T-029's UI must not be declared to satisfy T-C04 (validation remains a human gate with its own evidence package).
  - Audit findings (session 16, classified per the audit protocol): **A (blockers): none** — the authorization boundary was already enforced by SecurityConfig; the missing executable proof was closed pre-merge by `TeacherRouteSecurityIT` (test-only commit, CI-verified before merge). **B (non-blocking, documented):** web `loadKappa` renders non-404/409 errors (network/5xx) as the "No κ evaluation recorded yet" empty state — the Evaluate κ button still surfaces the real error; cosmetic honesty nit, not fixed to avoid scope creep. Stale `tab` value edge case if a session's role changed mid-login. **C (future work, NOT in this PR):** κ panel per-paper scoping UI (backend supports PAPER scope; the UI calls ALL scope only — tracked in the F-161 note); marking-queue pagination/ordering beyond pilot scale; roster → learner drill-down (§6.10).
- [ ] T-030 Research export (anonymized) + experiment registry v0. (F-146, F-154)
- [x] T-031 Render deployment (Docker, keep-alive, cold-start UX) + Vercel production env. (F-001, F-156) — **DEPLOYED + VERIFIED 2026-09-14 (session 67): operator credentials arrived; service LIVE at origin/main; runbook §3 checklist + learner journey 18/19 green on the deployed URLs (evidence: release t-031-prod-ingestion; the 1 failing item is the tutor LLM chain — the provider keys were wiped in a disclosed env-var incident and need operator re-provision; every other check incl. CORS, boundaries, contamination, papers browser passed; the canonical corpus is IN the prod DB — 81 papers/766 versions, verify 32/32). Remaining for full closure: re-provision SYLLABAI_GROQ/GEMINI/OPENROUTER_API_KEY (+ optional embedding key) in the Render dashboard.** History: configs + runbook MERGED 2026-09-08 (core PR #10) — **configs + runbook MERGED 2026-09-08 (core PR #10, branch `codex/t-031-pilot-deploy-configs`): `render.yaml` Blueprint (Docker, free plan, /actuator/health check, all secrets sync:false — Neon JDBC, JWT, CORS origins, LLM keys, R2) + `docs/DEPLOYMENT.md` pilot runbook (provision order, account policy incl. the one-time teacher provisioning path, the pre-pilot verification checklist = T-032 gate inputs, cold-start/decay/backup notes). The actual Render/Vercel deployment itself remains a manual operator step (credentials outside the repo) — this task stays open until the deployed URL passes the runbook checklist.** Session-20/21 note: the runbook's Vercel env-var instruction was corrected in core PR #11 (MERGED 2026-09-08; the old `.../api/v1` value doubled the prefix — browser-verified locally); the full student+teacher browser E2E has been executed once against a LOCAL live stack (evidence in `download/e2e-evidence/`), so the runbook checklist is now proven executable — re-run it against the DEPLOYED URLs after provisioning.
- [ ] T-032 Cycle-1 pilot readiness review against Master Spec §39a exit criteria.

## Wave 5 — pilot hardening (session 20, ship/verify/harden mode)

- [x] T-035 Merge the browser-E2E hardening PRs (HUMAN decision): core PR #11 + web PR #6 (`codex/pilot-e2e-hardening`) — **MERGED 2026-09-08 on explicit user authorization (session 21): pre-merge review (diffs read line-by-line, scope verified = exactly the four browser-found bugs, head-branch CI 34207204653/34207223589 SUCCESS), fast-forward merges preserving linear history — core `14cc866→c4af544`, web `99194af→e4a06cb` — both PRs closed+merged (GitHub API verified), post-merge CI log-inspected: core run 34228311619 on main @ `c4af544` = 270/270 unit + 30/30 IT across 10 IT suites incl. MisconceptionSurfaceFlowIT, BUILD SUCCESS; web run 34228321719 on main @ `e4a06cb` = lint + type-checked production build SUCCESS.** Fixes: MISCONCEPTION_OF read direction (misconception rule family + map annotations + trees were dead on every read surface), API base `/api/v1` normalization, honest auth 401s, structured-history badge, decay-test tolerance.
- [x] T-036 Provision the deployed environment (operator): Neon project + pgvector, Render blueprint deploy, Vercel project with the corrected env var, at least one free LLM key (Groq) for tutor/Smart Mark value paths, teacher account via the runbook's one-time provisioning path, then re-run the full browser E2E against the deployed URLs and record evidence. **Session-22 status (2026-09-08): deployment REHEARSED at production fidelity — Dockerfile-parity artifact built (JDK 25), prod-profile boot verified against external Postgres 17.11+pgvector (Flyway V1–V14 all-success, health UP in 16 s, R2-less + LLM-less boot proven post-fix), production web build + full browser E2E green (19/19 backend checks; adaptive loop re-proven with identical BKT/BDT numbers; isolation, teacher surface, honest error states; latencies measured). Two rehearsal-found bugs fixed + PR'd, CI-green, MERGE-PENDING: core PR #12 (R2-less boot failure — render.yaml hardcodes type=r2 while creds are optional) and web PR #7 (local-only demo credentials rendered on the production login screen). The workspace holds no cloud credentials (verified — none in env, no CLI tools, no config dirs): the cloud deploy itself remains REQUIRES OPERATOR ACTION — operator package with the exact sequence, safe-vs-secret env checklist, post-deploy verification commands and pilot failure procedures delivered at `download/t036/OPERATOR_DEPLOYMENT_PACKAGE.md`. Next: merge PRs #12 + #7 → execute the package → re-run the runbook checklist against the deployed URLs.** **Session-24 status (2026-09-10): BACKEND HALF DONE AND VERIFIED — Render service LIVE at `https://syllabai-core.onrender.com` (deploy 4, main `b35aa62`, CI green; boot 135.6 s, Flyway V1–V14, all three LLM providers registered — operator configured Groq + Gemini + OpenRouter keys; R2-less boot WARN as designed). Getting there fixed three distinct production-only bugs (deploy 1: nonexistent Maven base-image tag → `9d18303`; deploy 2: Groq client construction failed only when a real key is present — a path CI's zero-key boots never cover → `91441c0`; deploy 3: JVM footprint maxima ~850 MB vs the 512 MB free-instance cgroup → silent GC-thrash stall mid-Hibernate → port-scan timeout → small-container JVM recipe in the Dockerfile → `b35aa62`; full triage at `download/t036/RENDER_FAILED_DEPLOY_TRIAGE.md`). API verification 12/12 GREEN against the live URL (`scripts/t036_verify_deploy.py`): health UP, register/login/me, 8 seeded MCQs with no answer-key leakage, wrong answer → MIS-T1.1-01 misconception evidence, correct answer marked, attempt history read model, NBA 4 actions off the weak-mastery signal, teacher 403 / admin 403 / anonymous 401, tutor honest deterministic-refusal (no content ingested yet). CORS probed live: the allow-list is currently the DEFAULT (`http://localhost:3000,https://syllabai.vercel.app`) — the Vercel project must be named `syllabai` to match, or `SYLLABAI_CORS_ORIGINS` must be set in Render. REMAINING for T-036: (1) Vercel import + `NEXT_PUBLIC_API_BASE_URL` (operator, guided this session); (2) teacher account via the runbook one-time path; (3) full browser E2E re-run against the deployed URLs with recorded evidence.** **Session-24 follow-up (later 2026-09-10, operator pasted the live URL): Vercel half DONE — `https://syllabai-web.vercel.app` is LIVE (HTTP 200, Vercel edge, Next.js prerender, "SyllabAI — Learner Workbench"); the operator named the project `syllabai-web` (not the zero-CORS-change name `syllabai`) and set the env var correctly — `NEXT_PUBLIC_API_BASE_URL=https://syllabai-core.onrender.com` is verified BAKED INTO the deployed production bundle (direct JS-chunk inspection: `e3="https://syllabai-core.onrender.com".replace(/\/$/,"")`; the sandbox `XTransformPort=8080` fallback branch is present-but-dead). With that project name the CORS default does NOT match, and the preflight from the real origin was re-verified as an ACTUAL 403 (no ACAO) — control tests passed (`localhost:3000` → 200 + ACAO echo; `syllabai.vercel.app` → 200; no-origin → 401 auth challenge = app healthy), isolating the break to exactly one setting. THE CORS BLOCKER IS FIXED (2026-09-10, later still): operator set `SYLLABAI_CORS_ORIGINS` on Render; preflight from the real origin now returns 200 + `access-control-allow-origin: https://syllabai-web.vercel.app`, and a full browser-mode verification (Origin-attached preflight + register + login + 8 MCQs + wrong-answer attempt → MIS-T1.1-01 evidence + 403/401 boundaries) passed 11/11 (`scripts/t036_browser_mode_verify.py`). REMAINING for T-036: (2) teacher account via the runbook one-time path; (3) the human browser E2E pass over the six tabs with recorded evidence — the API-level path is now fully proven.** **Session-58 status (2026-09-13): DEPLOY PIPELINES BLOCKED — the four pilot-launch operator actions were executed as far as the sandbox permits. DONE: (4) pilot monitoring fully operational (web `pilot-monitor.yml` 6-hourly+weekly cron + `pilot_probe.py` → Discord, first real run 10/13 green with the 3 reds being exactly the stale deployments; TEST-classified monitor learner account + encrypted GitHub secrets; core `export_evidence.sh` + DEPLOYMENT.md §5 weekly evidence export). BLOCKED: (1) Vercel — every deployment since 03:18 UTC ends `"Deployment was blocked"` (account-level; 8 consecutive incl. canary 243a927 and the operator's 58891d3; git integration + CI + local build with all four pilot markers verified green); (3→2) Render — 12 consecutive deploy failures since 06:00 UTC (~2 min, code-independent; exact Dockerfile build green locally at aa5319e) so production runs STALE session-56 code (no teacher activation endpoint, no V16 migration, no f7adea7/5991187 fixes — proven by live probes); teacher account NOT sandbox-executable (Neon creds operator-only) — turnkey psql package at `download/session58-operator-package/OPERATOR_ACTIONS.md`. Production student loop re-verified in a real browser (marking → BDT signal → NBA remediation → retry → history, zero console errors). Flip path: Vercel unblock + Render clear-cache-deploy + teacher provisioning + activation click → monitor auto-green → READY WITH CONDITIONS.** **Session-59 status (2026-09-13, recovery directive with temporary Vercel/Render PATs): DEPLOYMENT RECOVERED — production runs current code (web efa7a80 bundle verified to contain all pilot markers; core 5f7897b, Flyway V1–V17 incl. V16 concept nodes), 4CH1 teacher KG ACTIVATED on production (idempotent 0/0, 153 validated edges), teacher account provisioned (pilot.teacher@syllabai-test.dev, TEACHER role, verified through the real app), both acceptance paths browser-verified (screenshots in download/recovery-2026-09-13-*), pilot monitor 13/13 green (dispatched run) / 14/14 with live teacher creds locally. Root causes fixed: Vercel git-author seatBlock (recovered via PAT-triggered deployment + machine-identity authoring for future pushes — the operator's personal-account pushes remain blocked until connected in the Vercel dashboard) and the Render Flyway V14 checksum mismatch (007e34d comment-edited the applied migration; applied bytes restored — AGENT.md Rule-1). REMAINING for T-036: (a) operator sets PILOT_TEACHER_EMAIL/PILOT_TEACHER_PASSWORD GitHub secrets (credentials in the session-59 report) so scheduled monitor runs do the teacher checks; (b) revoke both temporary PATs (confirmed no residual configuration). T-036 is otherwise CLOSED — the deployed environment is live, current, activated, and monitored.** **Session-60 status (2026-09-13): residual (a) CLOSED — the operator set both PILOT_TEACHER_* GitHub secrets (verified present by name via the API); the official monitor now runs the teacher checks ACTIVE: dispatched run 34771305305 = 14/14 green (teacher concept-graph read 200/153 edges + idempotent re-activation 0/0; all learner checks green; 1 advisory kappa N/A until T-C04). Scheduled 6-hourly + weekly crons verified active, secrets referenced correctly, zero credentials in repo files, zero plaintext in logs. Final deployment state recorded in the Session-60 WORKLOG entry: Vercel current (efa7a80) / Render current + health UP (5f7897b) / Flyway V14–V17 applied / 4CH1 teacher KG activated (idempotent) / 153 HUMAN_VALIDATED semantic edges active / teacher + student acceptance PASS / monitor GREEN with teacher checks ACTIVE. T-036 CLOSED — the only outstanding item is operator-side security hygiene: revoke the two session-59 temporary PATs (nothing on the sandbox side holds or configures them).**
- [x] T-036a (operator merge decision) Merge the two rehearsal-found fix PRs: core **#12** (`codex/t-036-r2-boot-fix` — lazy S3Client, boot WARN, loud per-op failure, 3 regression tests, render.yaml comment + DEPLOYMENT.md R2 section; CI 34243546661 SUCCESS) and web **#7** (`codex/t-036-login-demo-creds-prod` — NODE_ENV-gated demo-credentials box; CI 34244900527 SUCCESS). **MERGED 2026-09-09 on explicit user authorization ("Merge.") in session 23: core fast-forward c4af544→8e1f093, web fast-forward e4a06cb→376f4c5; 18/18 pre-merge checks (open/mergeable/head-sha/base-ancestor/CI-green/commits=1/main-unmoved); post-merge CI log-verified green (core run 34263167841, web run 34263199468).**
- [ ] T-037 Pilot data seeding decision: keep the validated V7 8-MCQ bank + teacher-validated structured content as the Cycle-1 practice surface; quarantine GLM-OCR corpus per T-C04; ~50 retake students self-register (STUDENT role by design); demo/test data stays `local`-profile-only and clearly separated.

## Content-ops track (parallel)

- [x] T-C01 GLM-OCR parser pipeline: real-corpus Markdown adapter + QP/MS extraction + mark reconciliation + image-reality handling + deterministic canonical identity + Python/Java conformance. (Session 8/9 — syllabai-parser @ `9eb35ab`, 68/68 tests + 12/12 conformance in CI; validation report `docs/validation/session9-real-corpus-validation.md`)
- [x] T-C02 GLM-OCR → core ingestion bridge: parser verified drafts → existing T-011 ingestion path → canonical persistence → T-013 chunk pipeline. — **COMPLETE and merged: PR #3 merged 2026-09-05 (fast-forward, no merge commit — main is now exactly `7c6f122`, the new content baseline)**. History: implemented on branch `codex/t-c02-glmocr-bridge` (CI run 33912946878: 185/185 unit + 16/16 IT incl. GlmOcrBridgeIT 6/6 on the 3 real WPH11 pairs; V13 bridge record keeps the verbatim parser contract — October Q18 + 1A 80-vs-120 conflicts review-visible; rerun-idempotent; embedding structurally separate; controlled entries = teacher endpoint + ops CLI; `docs/t-c02-bridge.md`); pre-merge correctness fix `7c6f122` "distinguish empty glm-ocr findings from missing bridge record" (missing bridge record → 404; existing record with empty `review_findings` → 200 `[]`) — 188/188 unit + CI run 33981175174 SUCCESS on the branch. **Post-merge verification on main @ `7c6f122`: CI run 33982065341 (push, main) — 188/188 unit + 17/17 IT (GlmOcrBridgeIT 7/7), BUILD SUCCESS; local `mvn test` 188/188 + `mvn verify` BUILD SUCCESS (ITs skipped locally, no Docker — as designed).** No new architecture; no embedding changes.
- [x] T-C03 ONE controlled Past-Papers batch end-to-end + human review, before any wider corpus run (the 3-pair proof is done; 40 batches stay parked until T-C03 review). Scope locked 2026-09-05: **a single bounded batch of real GLM-OCR QP/MS pairs through GLM-OCR → canonical documents → T-013 chunks → T-011 paper/question/mark-scheme model → bridge audit**; all imported content stays SUGGESTED; deterministic reruns (zero new rows); conflict preservation (never merged away); **no automatic embedding and no learner-facing serving**; batch audit report for human review before any wider run. Not a corpus-wide firehose. — **COMPLETE and merged 2026-09-06: parser PR #1 merged (main `9eb35ab` → `ba36ceb`, fast-forward; post-merge CI run 34015403577 — 71/71 + FULL conformance) + core PR #5 merged (main `7c6f122` → `1976540`, fast-forward; post-merge CI run 34015454011 — 196/196 unit + 22/22 IT incl. GlmOcrBatchIT 5/5, BUILD SUCCESS)**. History: implemented on branch `codex/t-c03-batch` in both repos (core PR #5 @ `4b820cc` CI 33983936464: 196/196 + 20/20 IT; parser PR #1 @ `ba36ceb` CI 33983496464: 71/71 + conformance). **Pre-merge review (session 12) found the product code sound but the two critical claims unproven by tests — added two regression ITs at `1976540` (branch CI 34015269882 green): (1) whole-batch atomicity — a late-failing pair (re-identified 1A copy whose canonical docs ingest fresh, then T-011 refuses the duplicate paper) rolls back the ENTIRE batch to pristine seed; (2) the real learner-serving boundary — QuestionController selection excludes all 59 imported ids while the 8 seed MCQs still serve, anchor-topic query empty, imported fetch 404, authoritative fetch serves.** Batch evidence (audit artifact, CI-verified): +6 canonical documents / +91 chunks (91/91 pending — no implicit embedding) / +3 papers / +59 versions / +59 schemes / +173 points / +3 bridge records, all SUGGESTED, second run zero new rows; June 20q/20p/51mp OK (80/80) findings 25; October 20/26/53 OK (80/null) findings 27 (Q18 visible); 1A 19/25/69 REVIEW_REQUIRED (80-vs-120 preserved) findings 32; all five invariants PASS; audit artifact downloadable from CI + workspace `download/t-c03-batch-audit-report.json`. **Next gate: T-C03 human content validation (see below) — only then any wider batch.** No new migration; no new HTTP endpoint; bridge untouched; PR #1 (T-026/T-027) untouched.
- [x] T-C05 Pre-Ingestion Standard + content-corpus architecture for the operator's manually prepared **Edexcel International GCSE Chemistry (4CH1)** corpus (specification / Student Book / past papers / Save My Exams notes & questions — operator converts manually with ocr.z.ai; SyllabAI does NOT convert and does NOT touch Cycle 1 IAL scope). Delivered as a contract-only design, no code: source-role model (5 roles, never flattened), **Corpus Markdown Convention (CMC v1.0)** for the manual conversion (front-matter schemas, heading grammar, chemistry notation rules, marks/MS conventions, assets & figure-missing markers), canonical metadata contract, chemistry-specific OCR lint vs human-review split, pipeline-stages × six-validation-axes lifecycle on the existing SUGGESTED→VALIDATED gate, four-tier spec-point mapping provenance (PUBLISHER/PROVIDER · AI_SUGGESTED · RULE_DERIVED · HUMAN_VALIDATED), evidence-based difficulty states (UNEVIDENCED→PROVISIONAL→EMERGING→EVIDENCED, per-CurriculumVersion population), provenance/versioning chain, five-level dedup semantics, `syllabai-corpus` repo layout, system-gap audit (SPEC_POINT node type + F-168 mapping provenance + `documents.kind`/`questions.provenance` enum extensions registered), and the **IAL↔IGCSE coexistence audit** (found: `ContentRetrievalService`/`ContentVectorRetriever` have NO curriculum scoping — registered as T-C07, a hard prerequisite before corpus embeddings are enabled; KG node codes must be namespaced `4CH1-*` because `knowledge_nodes.code` is globally unique). Doc: `CONTENT_CORPUS_ARCHITECTURE.md`. (Session 25, 2026-09-10) **Review pass on the first uploaded batch (same day, session 26): `syllabai-resources` reviewed and repaired (repo `8b9b5c3` → `c2b0e69`) — 5 expiring ocr.z.ai spec images rescued to local `assets/` (URLs died 2026-09-17), CMC front matter added to the spec md (`status: raw_ocr`), Web-Clipper `{pageTitle}` image bug repaired across all 112 SME notes (244/247 refs resolve; 3 figure-missing markers; re-runnable `scripts/c05_repair_clipper_refs.py` shipped in the repo), quality verdict: spec-point backbone intact (all sections + C-points + 12 practicals + Appendix 5 command words), chemistry notation degraded exactly as CMC predicted (CJK leak, lost sub/superscripts, full-width punctuation) — pipeline lint owns the fixes, not hand-editing; Student Book: convert the 383-page 1-up file, not the 192-page 2-up (page-level provenance). Findings + operator guidance: ` Same-session addendum: **Student Book OCR tooling decided (side task) — baidu/Unlimited-OCR (MIT, 25k stars) replaces ocr.z.ai for the 383-page image-only book.** Pilot verdict STRONG YES on 5 chemistry-stress pages via the HF demo Space (ion charges, subscripts, and full equations with state symbols preserved as LaTeX — vs ocr.z.ai's flattened CO32- on identical content); shipped c05_book_ocr.py (resumable per-page conversion driver, CMC front matter + 29-chapter map, two backends: official vLLM docker image for the full run on a rented GPU, HF Space for pilots — anonymous ZeroGPU quota empirically exhausts after ~8 calls); evidence + execution paths in syllabai-resources/BOOK_OCR_RUNBOOK.md + student-book-pilot/. Residual damage class ((l)→()/(1), Ar→A,, figure placeholders) stays lint-owned; LaTeX→HTML sub/sup normalization registered as a deterministic T-C06 converter step. (repo 53656a9)**
- [ ] T-C06 (PARKED — **precondition MET 2026-09-17: the starter corpus tranche has been live in production since session 67 and the T-C04 batches are closed — UNPARKED, kickoff pending; see the DB-reality addendum at the end of this row**) CMC→canonical converter + corpus lint: syllabai-parser adapter + core ingestion endpoint targeting canonical schema 1.0 + draft DTOs; `documents.kind` += TEXTBOOK/EXTERNAL_NOTES/EXTERNAL_QUESTIONS; `questions.provenance` += EXTERNAL_BANK; `SPEC_POINT` node type with namespaced codes (`4CH1-…`); F-168 mapping-provenance columns on question mapping; stimulus/figure attachment design. All corpus imports land SUGGESTED; nothing serves without human validation. **DB-reality addendum (2026-09-17, reconciliation pass — Neon production, read-only):** production now holds 94 exam papers (15 VALIDATED) / 854 question versions (127 VALIDATED, +8 vs the 09-16 check) / 172 documents (91 QUESTION_PAPER + 81 MARK_SCHEME) / 2,333 chunks (still ZERO embedded — embeddings stay gated on T-C07 curriculum scoping). Contract-vs-DB deltas for this row's deliverables: the 182/182 4CH1 spec-point nodes that serve today are VALIDATED but typed SUBTOPIC with namespaced 4CH1-dotted codes — the contracted `SPEC_POINT` node type is NOT in production (enum extension unshipped); `documents.kind` carries only QUESTION_PAPER/MARK_SCHEME (TEXTBOOK/EXTERNAL_NOTES/EXTERNAL_QUESTIONS absent); `questions.provenance` carries only PAST_PAPER/SEED_DEMO (EXTERNAL_BANK absent); no CMC→canonical converter or corpus-lint code exists in syllabai-parser or syllabai (tree-scanned 2026-09-17 — the parser's `canonical/` package is the pre-existing parse schema, not the CMC adapter); F-168 mapping-provenance columns likewise absent. Open deliverable unchanged: converter + lint + the three enum extensions + F-168 columns. **2026-09-17: PREPARED** — converter contract + enum migration draft V28 (documents.kind += TEXTBOOK/EXTERNAL_NOTES/EXTERNAL_QUESTIONS; questions.provenance += EXTERNAL_BANK; SPEC_POINT node type with a proposed retype-in-place backfill of the 182 SUBTOPIC-typed spec-point nodes; F-168 columns) committed in the package doc §4. Sequencing: after T-C07 (shared scoping joins), parallel with M2 arms; feeds the notes_mirror/figures substrates that gold classes 9/12 are N/A on today.
- [ ] T-C07 (REGISTERED from the T-C05 coexistence audit — pre-embedding prerequisite) Curriculum-scoped retrieval for the tutor evidence path: `documents`/`document_chunks` scope + vector query filter by the learner's active CurriculumVersion (index-time AND query-time); evidence source labels distinguishing spec/textbook/third-party providers. Required BEFORE any corpus embedding is enabled; today it is latent only because the GLM-OCR corpus is quarantined and unembedded. **2026-09-17: PREPARED** — implementation contract committed (`RETRIEVAL_FRONTIER_PREPARED_PACKAGE_2026-09-17.md` §1: join-path scoping predicate documents→exam_papers→subject/curriculum, fail-closed on unresolvable curriculum, negative-control ITs; one schema point — curriculum_versions↔subjects join — flagged for first-session verification). AF-1 strengthens this row's case: 85/119 VALIDATED question anchors point at ING-* nodes, so anchor resolution needs the same scoping predicate, not just chunk paths. No code committed (no Java toolchain in the authoring env) — status UNVERIFIED until a Maven lane runs the ITs. **2026-09-17 late: IMPLEMENTED in core `0d7dfaa` (Java lane, LOCAL VERIFIED)** — `CurriculumScope`+`CurriculumScopeResolver` (single-owning-ACTIVE-curriculum resolution: subject-root VALIDATED structure subtree OR exam-paper ownership; zero/ambiguous owners ⇒ empty scope ⇒ deterministic refusal), mandatory scope argument on every chunk-serving + KG-intent path (ports `KnowledgeRetriever`/`VectorRetriever` changed — no unscoped overload exists), `ChunkVectorRepository` gains a bound-parameter EXISTS predicate over the **DB-verified** join (the prepared package's guessed cv↔subjects direction was wrong: FK is `subjects.curriculum_version_id → curriculum_versions`; `exam_papers.question_paper_document_id` is varchar joining `documents.document_id`; 2,333/2,333 chunks resolve, 0 double-join), KG intent surface = subject-root PART_OF subtree (DB-verified: covers all 226 VALIDATED structure nodes, zero outside — structurally excludes ING-*/WCH11- scrape nodes, AF-1 containment); KaRagService resolves once per ask, unresolved ⇒ refusal with telemetry; CLA scopes per its owning-subject house rule. Verified: 604 unit tests green (resolver policy, scope filtering, unscoped-refusal, SQL predicate marker); ContentPipelineIT now carries the T-C07 positive + negative controls (foreign-curriculum scope, unlinked document) — Docker-ITs remain on the CI route; production SQL predicate executed read-only on the real DB (4CH1-2017 scope = 2,333 chunks, IAL = 0, random = 0); harness replay of Run-002 vs recorded evidence byte-identical (ff959b40) — scoping provably does not perturb A0. Residual (unchanged scope): evidence source labels (spec/textbook/third-party) not in this change; per-learner curriculum selector is the documented follow-up the ambiguous-owner refusal forces.
- [ ] T-C13 (REGISTERED 2026-09-17 from the retrieval doc-gap audit — the ADR-020 benchmark gate) Retrieval benchmark harness over the 4CH1 pilot corpus, per `RAG_RETRIEVAL_RESEARCH.md` §6 as extended by `GEMINI_FILE_SEARCH_AND_NOTEBOOK_ARCHITECTURE_RESEARCH.md` §16: 100–200 gold-labeled tutor queries across the 12 query classes (factual / conceptual / calculation / prerequisite / misconception / why-wrong / exam-question / mark-scheme / revision-note / vague learner language / multi-SpecPoint / diagram-dependent); comparison arms A–G (A pgvector semantic baseline · B BM25/lexical · C hybrid · D hybrid+reranker · E Gemini File Search · F hybrid+FS · G KG-aware hybrid+FS); metrics Recall@5/10/20, MRR, nDCG, SpecificationPoint resolution accuracy, evidence precision, false-positive rate + grounding (evidence sufficiency, citation correctness, claim-to-evidence alignment, unsupported-claim rate) + ops (p50/p95 latency, query cost, index footprint); gold labels derive from the 209 HUMAN_VALIDATED note→point mappings + VALIDATED question versions; provider-specific tests per FS doc §16 (metadata filtering accuracy, document-identity fidelity, page/figure fidelity, corpus update behavior, validation boundary, subject isolation, scale, portability). **This row is the promotion gate for every retrieval technique (ADR-020 acceptance principle; integration rule 12 "benchmark before promotion") — no retrieval change becomes a production default without it.** Registration basis: TODO/PROGRESS contained zero references to the benchmark (Recall@/MRR/nDCG absent) despite two ACCEPTED research docs gating production promotion on it; DB-verified preconditions live in production Neon (182 distinct 4CH1 spec-point nodes, 112 VALIDATED prerequisite edges, 2,333 chunks — embeddings still 0, gated by T-C07). **2026-09-17: spec committed — `RETRIEVAL_BENCHMARK_HARNESS_SPEC.md` (`c2126f7`), Draft v0.1 awaiting owner ratification of §10 (acceptance thresholds, set size 120 vs 200, labeling ownership, M2 judge budget, B-proxy legitimacy, H2 arm timing). M0 (gold set authoring + freeze) and M1 (harness + A0 baseline + B-proxy, Run 1 on record) are startable immediately post-ratification and deliberately do NOT block on T-C14/T-C15 — arms light up in recorded runs on the same frozen v1 set as their prerequisite rows land.** **2026-09-17 execution lane (M0 DONE, M1 PARTIAL — LOCAL VERIFIED, offline+deterministic):** gold set **gold-v1 FROZEN** (120 queries / 12 classes; per-class files + manifest + SHA256SUMS under `bench/gold/`; label rules R1/R2/R3 recorded per record; anti-leakage freeze before Run 1; quota amendment: calculation 4 cue-matched + 6 numeric-stem fill because `questions.command_word` is 119/119 NULL). Snapshot **snap-001** committed (`evidence/bench-001/snapshot/` — 2,333 chunks portable identity, 182 spec points, 152 VALIDATED non-structural edges, graph-as-code 98 concepts + 19 misconceptions pinned at syllabai-resources@b2bff3f). `gold_check` PASS incl. 6/6 negative corruption classes. **Run 1 (B-proxy) RECORDED** (`evidence/bench-001/runs/run-001-bproxy/`) — ALL-chunks Recall@10 0.2954 / MRR 0.2464 / nDCG@10 0.4299 vs VALIDATED-only 0.1449 / 0.2330 / 0.3237 = AF-2 (the validation boundary halves lexical retrieval). A0 baseline + B/C arms remain for a Java 25/Maven lane (harness pieces PREPARED in the package doc). Audit findings AF-1/AF-3 in `RETRIEVAL_FRONTIER_PREPARED_PACKAGE_2026-09-17.md`. **2026-09-17: SPEC RATIFIED — §10 rulings 1–6 recorded in the spec (now v1.0): §8 thresholds binding unchanged (+ mandatory dual-denominator ALL vs VALIDATED-only reporting in every run report, gate arithmetic on ALL); gold-v1 @ 120 stands (v2→200 only on demonstrated per-class need); auto-derived labels + operator spot-check (20% stratified + 100% classes 4/5/6; <90% class precision → manual re-author before next run — owner action item); M2 judge budget capped (free chain, 80 calls/run, ceiling 120, no paid judging without re-ratification); B-proxy confirmed legitimate early-signal/diagnostics instrument (never promotion-eligible); H2 stays behind the C/D verdict. Ratification retroactively adopts gold-v1/snap-001/Run-001 as the v1.0 basis (timeline-honesty note in spec §10) — no frozen file changed. Next: Java-lane M1 completion (A0/B/C) — PREPARED, UNVERIFIED; T-C07 merge gate before embeddings. 2026-09-17 late: agent lane BOOTSTRAPPED (Temurin JDK 25.0.4 + Maven 3.9.11 user-space; core 7eb621a test-compile green, unit tests execute green) — M1 completion is now writable from the agent lane; Docker-ITs still need Docker (CI route).** **2026-09-17 Run 002 (A0) RECORDED — M1 DONE:** harness live in syllabai-core `src/test/java/com/syllabai/bench/` (core `9690c00`, LOCAL VERIFIED: 11/11 harness tests; production GraphKnowledgeRetriever + ReciprocalRankFusion run UNMODIFIED over snapshot-backed repo stubs; fail-closed SHA-256 loaders; metrics pinned line-for-line to Run-001 formulas). Spec-resolution axis (n=120, A0's native output): coverage 0.5569 · exact-hit 0.5083 · matched_ratio 0.60 · precision-over-matched 0.2944; per-class: factual/conceptual/vague/notes 1.0 (R2 authoring circularity caveat recorded), prerequisite 0.69, misconception 0.65, stem-derived classes 0.0. Chunk axis = REAL ZEROS per ratified spec §6 (A0 emits no chunks at 0/2,333 embedded — the engine-without-fuel baseline on record); zero VALIDATION_BOUNDARY_VIOLATIONS; determinism double-run PASS. **Finding (durable):** prerequisite + misconception signals fire ZERO times across 492 matched topics — the settled pedagogy edges live on 4CH1-CON-* concept nodes, all SUGGESTED (invisible to production matching) with 0 VALIDATED attachment rows bridging to spec points; the tutor's pedagogy context is structurally unreachable in production today (arm I / T-C06+T-C11 lane work). Evidence: `evidence/bench-001/runs/run-002-a0/`. Remaining for T-C13 exit: arms A/B/C (T-C07 + embeddings + T-C14) + owner acceptance of the first promotion verdict.** **2026-09-17 run-003-b (arm B recorded, session 86):** B = production lexical arm, RUNNABLE through the fabric contract over flyway-migrated PG + snap-001; A0 untouched (chunk-axis real zeros stand); A/C/D/E/F/G/H1/H2/H3/I honestly UNAVAILABLE. B reads BELOW the B-proxy probe on the VALIDATED-only corpus (ts_rank_cd coverage vs Okapi IDF-sum — different scorers, honestly recorded for the hybrid-C verdict, no promotion argument either way); query-form diagnostic (AND vs OR) routed to the dev-subset/gold-v2 route per the anti-tuning rule.
- [ ] T-C14 (REGISTERED 2026-09-17 from the retrieval doc-gap audit — P0 per `RAG_RETRIEVAL_RESEARCH.md` §7) Provider-neutral retrieval contracts + the missing lexical arm: `RetrievalProvider` port (`StructuredRetrievalQuery` → `RetrievalCandidate`s) unifying the existing `VectorRetriever` + `KnowledgeRetriever` behind one contract and adding the BM25/lexical provider (Postgres FTS candidate — no new infrastructure, free-tier preserved); today's RRF fusion covers only 2 of the research doc's 4 candidate-generation arms (semantic + KG; lexical and metadata-filter arms absent); lexical carries no contract, no implementation, and previously no tracker row. Benchmark arm B/C/D of T-C13 decides promotion; reranking stays behind `EvidenceReranker` (NoReranker until benchmark evidence). Honesty constraints unchanged: deterministic fusion, embeddings never educational truth, no vendor-specific domain model in the contract (§8 rules 2/4/10). **2026-09-17: PREPARED** — migration draft V27 (generated `content_tsv` tsvector column + GIN) and `Bm25Retriever`/`RetrievalProvider` ratification checklist committed in the package doc §3; fused by the existing `ReciprocalRankFusion` (k=60) — no new fusion code. Run-001 B-proxy gives the early lexical profile (formal spec-title queries weak, stem-verbatim strong) for comparison once arm B lands. Code UNVERIFIED until a Maven lane compiles + ITs green. **2026-09-17 IMPLEMENTED + RECORDED (session 86, reconciled onto the retrieval-fabric lane's `99be333`):** the `RetrievalProvider` fabric (`StructuredRetrievalQuery`/`RetrievalCandidate`), `ChunkLexicalRepository` (V28 tsvector+GIN) and the `Bm25Retriever` arm landed by the concurrent fabric lane and kept as the base (duplicates dropped, no overwriting); this session added the **T-C05 serving-eligible boundary** (`searchServingEligible` — owning paper must be VALIDATED, so SUGGESTED/FLAGGED/REJECTED chunks are unreachable; `LexicalBoundaryIT` proves suggested→never-served, validate→served, flag→gated through the arm in real Docker CI run `35162299609`) and the **benchmark arm-B record** (`bench.ArmB`/`Run003B`: the production provider over a flyway-migrated PG loaded with the frozen snap-001 corpus; run-003-b RECORDED — recall@5/10/20 0.074, mrr 0.1224, ndcg@10 0.1224 on gold-v1's 98 labeled queries, ZERO validation-boundary violations, determinism double-pass PASS; spec-resolution axis NOT SCOREABLE — zero HUMAN_VALIDATED chunk→spec mapping rows, named data gap). Query-form finding routed honestly: the bare-word AND form leaves 106/120 queries zero-candidate; a term-union diagnostic measured better in-session but was NOT adopted against the frozen set (anti-tuning rule) — dev-subset/gold-v2 decision. **IMPLEMENTED + RECORDED; NOT a production serving default; promotion stays behind the T-C13 gate.** **2026-09-17: IMPLEMENTED (lexical arm + fabric contract, core `99be333`, LOCAL VERIFIED)** — the prepared package's migration draft V27 was RENUMBERED to **V28** (`V28__content_lexical_search.sql`: `content_tsv` GENERATED ALWAYS tsvector + GIN; additive, reverse SQL recorded in the header — V27 had been taken by the revision-notes lane, and the T-C06 draft number V28 is consumed with it, so T-C06's enum migration becomes V29+ when it lands). The `RetrievalProvider` fabric landed per the ratified-on-merge sketch with ONE recorded deviation: `StructuredRetrievalQuery` carries the resolved `CurriculumScope` (T-C07's canonical scope carrier, incl. the KG intent surface the KG adapter needs) instead of a bare curriculum-version id; `LearnerSignals`/`EvidenceRequirements` are sketch-shaped placeholders (Phase G). `Bm25Retriever` implements the contract exactly: `websearch_to_tsquery('english', q)`, `ts_rank_cd` ordering with a deterministic chunk-id tiebreak, the T-C07 EXISTS curriculum predicate as a bound parameter, blank-query fail-closed-empty before any SQL, candidate limit from the query record; `ChunkLexicalRepository` owns the native SQL (ChunkVectorRepository posture). Adapters for the two existing ports (`PgVectorRetrievalProvider`, `AuthoritativeKgRetrievalProvider` — topics only, pedagogy context never a candidate) + the `GeminiFileSearchRetriever` contract stub (`available()=false`, deliberately not a bean — T-C15) complete the arm-registry surface. Tests: 27 new unit tests (SQL shape + fail-closed contracts + mapping fidelity) green in the bootstrap lane; full suite 631/631 with 1 Docker skip at `48db7f7`; `ChunkLexicalSearchIT` (stem-verbatim top-5 ranking sanity, foreign-curriculum + unlinked-document negative controls, deterministic ordering) rides the CI lane with the other Docker-ITs. Honest scope: **serving wiring untouched** — BM25 becomes a served candidate source only through the T-C13 benchmark verdict; an arm-B run additionally needs V28 applied (CI/production flyway), which the agent sandbox cannot do. Side note: this lane surfaced the latent Mockito defect in the T-C07 `CurriculumScopeResolverTest.ambiguousOwnersRefuse` (nested same-mock call inside a thenReturn argument); fixed upstream in `d191ea9` while this lane worked — adopted.
- [ ] T-C15 (REGISTERED 2026-09-17 from the retrieval doc-gap audit — executes the explicit next action in `GEMINI_FILE_SEARCH_AND_NOTEBOOK_ARCHITECTURE_RESEARCH.md` §20, doc dated 2026-09-14) Gemini File Search POC + benchmark arm: minimal corpus slice per Experiment A (a few HUMAN_VALIDATED revision notes + validated exam questions + matching mark schemes + one supplementary Class-C teacher PDF), SyllabAI metadata projection (≤20 custom fields: resource id/version, curriculum scope, spec point ids, validation status), citation normalization into the SyllabAI evidence chain (FS citation = input to evidence handling, never final provenance); benchmark arms E/F/G ride T-C13; authority boundary absolute — FS never becomes authoritative for curriculum/spec points/prerequisites/misconceptions/mastery (§5), never the sole durable representation (§14-G portability), no production default until benchmark evidence. Registration basis: zero references in TODO/PROGRESS/AGENT/MASTER_SPEC despite "Yes, investigate immediately" in the doc's §18 decision matrix.
- [ ] T-C16 (REGISTERED 2026-09-17 from the Path-3 parsing-flow assessment — corpus acquisition/organization tooling; **DESIGN RATIFIED 2026-09-17** via owner-delegated self-review, ratification record in spec §12 — legacy manifest shape re-verified against `paper 1/MANIFEST.json` (sessions was always a keyed dict — §7 corrected), image-island form pinned to the verified single-line center-div shape, `--from-staging` promoted into the scrub CLI contract, filename-collision + duplicate-document fail-closed rules added) `tools/corpus_ops/` in syllabai-parser: makes the Stage A/A′ loop a committed, tested artifact instead of agent folklore (the 2026-09-11…13 `paper 1/` cycle — same-second download of 2,002 crops, `operator_cleanup` of 1,362 images with reference scrub, `structural_repair` of 7 files, `operator_cleanup_2` — was executed ad-hoc and the tooling never committed). Four commands: `intake` (retro image rescue from already-converted markdown before ~1-week signed-URL expiry + pair organization into the `Past-Papers` per-session layout, operator-confirmed pairing — never content-inferred identity), `scrub` (deletion-manifest-driven reference removal, grammar-aware, fail-closed, orphan detection hard-Fails), `verify` (read-only manifest↔filesystem agreement), `rename` (provisional `UNIDENTIFIED-*` sessions fixed via the same proposal/confirm pattern). MANIFEST schema v1.1 additive (legacy `paper 1/MANIFEST.json` keys canonical + `ops_log[]` + per-session `clean{}` derivative block; v1.1 additions narrowed at ratification to `mime`/dimensions/document `size` — `images` keys already are the source URLs); deterministic writer; mocked-HTTP + fixture-tree CI tests; `clean_diff.py` (Stage B gate G3 implementation) rides this package (synthetic-pair tested now; corpus-proven only after Stage B's first real pair). Spec ratified: `CORPUS_OPS_TOOLING_DESIGN.md` (v1.1). **IMPLEMENTED 2026-09-17**: `tools/corpus_ops/` committed to syllabai-parser (intake/scrub/verify/rename + clean_diff + mocked-HTTP & fixture-tree unit tests, all green). Rollout gate owner-held: dogfood on the next ocr.z.ai conversion batch BEFORE it touches `Past-Papers`; replay the historical `paper 1/` manifest as the integration oracle.
- [ ] T-C17 (REGISTERED 2026-09-17 from the Path-3 parsing-flow assessment — the Stage B clean-and-verify protocol) Subtractive, grammar-preserving clean-and-verify pass over QP/MS GLM-OCR markdown BEFORE extraction: removes covers/instructions/formula-sheet/periodic-table/marking-preamble boilerplate (the known, mechanical, located cause of the "boilerplate spillover" defect class — real 4CH0/1C Jan 2012 pair yields 27 draft questions from a 20-question paper), applies the §7.1 bounded fix taxonomy (fence balancing, unambiguous table closure, byte-identical page-split dedup), and escalates everything else to the operator via `clean-proposals.json`. **Grammar contract RATIFIED 2026-09-17 against `GlmOcrMarkdownParser` v1.2.0 source (syllabai-parser tree `5c93317`): 10 invariants anchored to the parser's actual regexes/bounded-scan predicates — including image-island exact single-line shape (a reformatted image line silently degrades to a raw-HTML paragraph), QWC-asterisk/list-item interaction (`*(c)` survives, `* (c)` reclassifies), entity-decode ownership (cleaning never pre-decodes), P-11 `$$` span discipline — and gate G5 upgraded to the parser's provenance honesty counters (`unterminatedTableBlocks`/`orphanMathFences`/`unclosedCenterDivs`/`greedyMathLines` must be absent from the clean parse).** Acceptance gates G1–G5 (pair CLI end-to-end; health.py zero FAIL; raw-vs-clean draft diff with mark-identity invariant + element-id shift expected; report completeness; grammar counters) block ingestion of any un-CLEANED pair; cleaning never changes validation state (SUGGESTED/reviewRequired untouched). Artifacts per session: `clean/{QP,MS}.md + clean-report.json (+ clean-proposals.json)`; raw stays the ADR-021 provenance root. Spec committed: `CLEAN_VERIFY_PROTOCOL_QP_MS_MARKDOWN.md` (v1.1); operational brief for the executing agent: `STAGE_B_AGENT_TASK_BRIEF.md`.
- [ ] T-C19 (REGISTERED 2026-09-17 from the T-C13 Run-002 A0 finding + run-003-b reconciliation, session 86) Concept-node mapping/validation lane: the settled pedagogy edge store (112 REQUIRES_PREREQUISITE + 14 REMEDIATED_BY + 13 WRONG_ANSWER_PATTERN + 2 COMMONLY_CONFUSED_WITH, all HUMAN_VALIDATED) attaches to `4CH1-CON-*` concept nodes that are ALL SUGGESTED and carry ZERO HUMAN_VALIDATED attachment rows bridging them to the spec-point serving surface (snapshot concept_attachments = 0) — so A0 never emits pedagogy context. **Classification (session 86): GRAPH-MAPPING GAP** — the missing validated concept→spec-point mapping substrate; the SUGGESTED state itself is correct governance and must NOT be "fixed" by promotion. NOT a retrieval gap (the governed surfaces behave as designed), NOT intent-matching (A0 matched_ratio 0.60), NOT benchmark-authoring (gold honestly mirrors the served state). Fix path: validated mapping rows (T-C06/F-168 four-tier provenance family) + the validation lane; surfacing then rides arm I / KG-expansion with its own benchmark record. Scope guard: never make SUGGESTED nodes servable to close the gap.
- [ ] T-C18 (REGISTERED 2026-09-17 from the Path-3 parsing-flow assessment — post-review metadata completion so Test Builder filtering becomes real; **DESIGN RATIFIED 2026-09-17** via owner-delegated self-review, ratification record in spec §11 — one material correction: a type substrate ALREADY exists (`Question.Type {MCQ_SINGLE, SHORT_ANSWER, STRUCTURED}`, hardcoded `STRUCTURED` at ingestion and load-bearing in STRUCTURED-gated serving paths), so §3 v1.1 replaces the proposed version-level `question_type` column with additive `detected_type`/`type_source`/`type_state` on `Question` (signal persisted, serving semantics untouched, MCQ promotion explicitly rides the MCQ end-to-end project); two-axis source/state rule clarified for §6 "EVIDENCE-validated"; migrations corrected V27-current → next is V28) Closes the three verified gaps blocking "filter by difficulty/type/syllabus point" (F-050): (1) `difficulty` is hardcoded `3` at ingestion (`PastPaperIngestionService` "difficulty unknown until review") — becomes nullable + `difficulty_source` enum (UNKNOWN|SME|EVIDENCE|HEURISTIC) with backfill `UNKNOWN`, SME authoritative, evidence-derived values are always proposal-only (SUGGESTED, never self-applied), heuristic bands excluded from serving filters; (2) the parser's MCQ signal dies at the ingestion boundary — §3 v1.1 (post-ratification): persist it as additive `detected_type`/`type_source`/`type_state` on `Question` (the existing serving `Question.type` column is untouched; see the ratified prefix above); (3) no question↔spec-point relation in core DB (T-C12 tagger output lives corpus-side only) — new `question_spec_points` table (PRIMARY/SECONDARY, SUGGESTED→VALIDATED, provenance jsonb, unique constraint) fed by a fail-closed T-C12 importer with curriculum-family scoping (4CH1↔IGCSE family; cross-family rejected loudly). Test Builder contract §6: filters consume only SME/EVIDENCE-validated sources; UNKNOWN excluded from candidate pools, never approximated; empty pools are explicit blueprint failures. Additive-only Flyway (V28 next — V27 verified current at ratification). Sub-rows T-C18a–e in the spec §10 (T-C18a rescoped per §11: V28 = nullable difficulty + source/state + detected-type columns on Question + `question_spec_points`). Spec ratified: `POST_REVIEW_METADATA_COMPLETION.md` (v1.1).
- [ ] T-C08 (future) Evidence-based difficulty states wired into serving policies: per-(question part, CurriculumVersion population) statistics jobs (p̂, discrimination, response time, sample state); recommendation/mock weighting by evidence state; new-question exploration share.
- [x] T-C09 (KG build Phase 1) Deterministic specification-skeleton extraction from the 4CH1 Issue 3 spec md → graph-as-code in syllabai-resources. — **COMPLETE (session 28, 2026-09-11; repo `c09-phase1`): `scripts/c09_spec_graph_extract.py` (deterministic, zero-LLM, hard-gated) + `scripts/graph_check.py` (validator, 8 check groups, negative-tested against 8 corruption classes) + `graph/` = 6 YAML files (182 specification_points · 4 topics + 28 subtopics · 210 PART_OF edges · 25 command words · 12 practicals · 3 AOs + 2 papers) + 2 reports (`PHASE1_COMPLETENESS.md`, seeded 20-statement `SPOT_CHECK_SHEET.md`). Ground truth amended by the statement-level PDF reconciliation: **182 unique codes (S1:60/S2:50/S3:22/S4:50) / 52 C-points / 28 subsections** — supersedes the plan's §5 167/40/29 baseline (evidence in PHASE1_COMPLETENESS §2; the md holds all 182 but 4.49C/4.50C are OCR-orphans outside the HTML tables). Cross-check: 119 exact / 52 normalized / 11 fuzzy (residual OCR damage) / **0 mismatch**, statement order identical, md↔PDF code sets equal; damage policy honored (verbatim text, 54/182 flagged, never fixed; 4SD0 whitelisted only inside the Double-Award applicability rule). Two real defects found by the gated first run and fixed in the extractor: (1) PDF-walk listing pollution — the section-start sub-topic TOC listings glued onto the previous section's last statement (1.60C/2.50 mismatches + 3.22C hidden fuzzy false-pass) → listing-state machine; (2) table-classifier ordering — Appendix 1's 'Paper codes' row substring-matched `paper_overview` before `codes_appendix`, emitting a bogus third paper record → check reorder + `papers: 2` gate. Per-node provenance: issue + md line anchor + table/row index + row shape + PDF page + match level; edge vocabulary = live V2 enum (PART_OF only, names never invented); `4CH1-*` namespace enforced everywhere. DB wiring rides T-C06 (CurriculumDraftDto → SPEC_POINT, built in T-010). Operator gates: SPOT_CHECK_SHEET (20 statements) + git PR review = the HUMAN_VALIDATED promotion.**
- [x] T-C10 (KG build Phase 2 — **COMPLETE 2026-09-11, session 29; spot-check sheet operator-reviewed 2026-09-11: 19/20 confirmed (one after machine visual verification of the lattice diagram) + 1 REJECTED (4CH1-4.15) remapped to the Nitrogen Oxides & Sulfur Dioxide note (see scripts/c10_rework_415.py + the sheet review record); remaining gate: the git PR front-matter review (HUMAN_VALIDATED promotion)**: all 112 SME notes carry a `spec_map:` front-matter block = deterministic PROVIDER subsection anchor (source-URL slug; 28/28 groups align 1:1 with the spec subsections, ordering exact, verified programmatically) + **211 AI_SUGGESTED point-level mappings** (high 176 / medium 34 / low 1), each with a verbatim evidence quote + confidence + model version + rationale + `validation_status: SUGGESTED`. Hard gates ALL GREEN: evidence-verbatim-in-note anti-hallucination gate (caught 2 quote defects during the build); every code ∈ the 182-point registry; every note ≥1 mapping; note bodies byte-identical (+3242 front-matter lines / 0 deletions); idempotent apply. Coverage: **182/182 points have ≥1 direct mapping — the Phase-3/4 zero-coverage queue is EMPTY** (quality gaps still flagged, e.g. poly(tetrafluoroethene) missing from the polymer note's examples). Validator: `graph_check.py` group 6 `c10-notes-mapping` (9 groups ALL PASS) + 8 new corruption classes negative-tested. Reports: `graph/reports/PHASE2_MAPPING_COVERAGE.md` (method, per-subsection + per-point detail, 1 cross-subsection flag 4CH1-1.17, PR review guide) + `PHASE2_SPOT_CHECK_SHEET.md` (20 sampled mappings). Tools: `scripts/c10_worksheets.py`, `scripts/c10_map_notes.py`, `scripts/c10_decisions/S*.json` (the AI decision record), `scripts/c10_negative_test.py`. DB wiring still rides T-C06; zero core changes. Semantics per `KNOWLEDGE_GRAPH_CONTEXT.md` §8A.7: PROVIDER slug signal ranked above semantic similarity, uncertainty preserved per mapping. **PR-review tooling (2026-09-11, session 31): the remaining operator gate is operationalised** as `graph/reports/PHASE2_PR_REVIEW_GUIDE.md` (deterministic work order in the operator's priority sequence: remapped 4.15 → the 1 low → the 34 medium with cross-note-deferral flags first → the 1.17 cross-subsection flag → the deferral/semantic-completeness scan → the diagram-dependent queue incl. the 3 figure-missing notes); **promotion pathway is decisions-side** (per-mapping `validation` blocks in `scripts/c10_decisions/S*.json`, gated by applier G7, carried into front matter as HUMAN_VALIDATED + validated_by + validated_date — hand-editing note front matter would be silently reverted by the next applier re-run); `scripts/c10_promote.py` batches promote + gated re-apply; validator c10.3 accepts exactly SUGGESTED or a complete HUMAN_VALIDATED block (negative-tested 10 classes + positive control); promotion rehearsal executed end-to-end then fully reverted — on-disk state is 0 promoted / 211 SUGGESTED, awaiting the operator's PR review) **; PR-review EXECUTED 2026-09-11 (session 32, AI review pass under the operator's clarified mapping contract): `graph/reports/PHASE2_PR_REVIEW_SHEET.md` records 61 distinct mappings reviewed (4.15, 1.4, all 34 mediums, both 1.17s, the 21-mapping diagram queue with VLM verdicts archived in `scripts/c10_vlm_results/`, the 7 missing-figure mappings) — 61 CONFIRM / 0 REJECT / 0 HOLD under the §0.0 contributory many-to-many contract (SME notes group and split spec points; 69/112 notes carry 2+ points, 25/182 points covered by 2-3 notes); 6 rationales reworked for honest contributory wording (4.15, 1.4, 1.16@Atoms, 2.29, 3.10, 1.17@S1-e) with evidence/confidence/tier/validation unchanged; all gates re-run green, note bodies byte-identical; the one remaining step to close T-C10 is the operator's ratification command (staged, validated 61-spec batch, sheet §11) — until then on-disk state stays 0 promoted / 211 SUGGESTED. **Ratification audit (2026-09-11, session 33): the operator-advisor's pre-execution condition is DISCHARGED — `scripts/c10_ratify_audit.py` (read-only, resolves the §11 specs with the PRODUCTION `c10_promote.py` resolver) proves the batch promotes exactly the 61 reviewed CONFIRM pairs: bijection 61/61 both directions, 30 bare + 31 @FRAGMENT specs all unambiguous (zero code-wide promotion, zero unreviewed targets), pre-state 0 promoted / 211 SUGGESTED; 10/10 checks PASS, report `graph/reports/C10_RATIFICATION_AUDIT.{json,md}` — the §11 batch is machine-verified ready to run**** **; ROUND-4 REWORK (2026-09-11, session 34): the advisor's fourth review round rejected 2 of the 61 confirms — 4CH1-4.15 (explain-how point: no corpus note teaches the impurity→SO₂ formation explanation; the round-3 CONFIRM had aggregated premise + stated result = inferred coverage) and 4CH1-1.17 @ Calculate Relative Mass (calculate point: definitional derivation-basis sentence only, duplicated in the S1-c note) — both removed via `scripts/c10_round4_rejects.py` (idempotent): store 211→209 (176-32-1, 68/112 multi-point), coverage 182→181 with 4.15 an ANNOTATED zero-coverage corpus gap (T-C11 input: pieces exist, connective teaching does not), the command-kind substance rule frozen as guide §8, review sheet §12 superseding §1/§11 with the staged 59-spec batch (§11 marked ON HOLD), all gates re-run green (applier 209, graph_check 9/9 at 112/209/181, negative test 10+PC); the re-targeted ratification audit passes 12/12 (C1b/C4c new: both REJECTs proven removed from the store and absent from the command; bijection 59/59) — the ONE remaining step: the operator runs the sheet §12 command → 59 HUMAN_VALIDATED, 150 stay SUGGESTED risk-tier, 4.15 stays an honest corpus gap**** **Round-5 addendum (2026-09-11, session 35): the remaining 150 mappings are now formally AI-reviewed — 150 CONFIRM / 0 REJECT / 0 HOLD under the frozen contract (full-note reads, 8 VLM diagram checks, 18 adversarial flags, 5 corpus-level partial-coverage observations for T-C11; artifacts `C10_ROUND5_REVIEW.json` + `PHASE2_ROUND5_REVIEW_SHEET.md`; store untouched at 209/0-promoted/all-SUGGESTED; resources c6454c9 → c90f5ae). Operator decisions pending: ratify §12's 59-spec batch; ratify the 150 (single controlled promotion or risk/section split).****** **Session-37 addendum (2026-09-11): T-C10 CLOSED — operator ratification executed ("Ratify the 59 and promote 150"): the §12 59-spec command + the §13 150-spec staged batch (derived mechanically from the round-5 review JSON; dual-batch audit pre 17/17 PASS) both executed verbatim → **209/209 mappings HUMAN_VALIDATED** (validated_by `operator`, 2026-09-11; review trail rounds 3–5); post-execution audit 7/7 PASS (promoted == staged union exactly; all 150 evidence quotes retained byte-for-byte; the 2 rationale corrections applied verbatim — applied before promotion in resources commit b0008d4; all 112 note bodies byte-identical to the round-5 review state c90f5ae); gates green (applier `promoted HUMAN_VALIDATED: 209`, graph_check 9/9, negative test 10 classes + positive control with state-agnostic promotion corruptions); resources b0008d4 → 59b8207 (rebased over the operator's PMT mark-schemes push). T-C11 may begin; 4.15 stays the honest annotated zero-coverage corpus gap.** 
- [ ] T-C11 (KG build Phase 3 — after T-C10) Concept/prerequisite/misconception graph: canonical CONCEPT nodes + alias tables under a split-first identity policy (over-splitting is recoverable, over-merging corrupts mastery attribution; merges are human-only); PREREQ_OF edges enter as AI_SUGGESTED with evidence anchors and serve only after HUMAN_VALIDATED promotion (recursive-CTE closure already live in core); MISCONCEPTION nodes require source quotes (SME text / question distractors), and the triple distinction `COMMONLY_CONFUSED_WITH` ≠ `MISCONCEPTION_OF` ≠ `WRONG_ANSWER_PATTERN` is modeled separately (§8A.11); Student Book page-level `EXPLAINED_BY` enrichment after the full 383-page OCR run (chapter map already in the driver's CMC front matter). Gates: no orphan concepts; every edge provenance + evidence anchored; 100% of misconceptions source-quoted. Phase 4 (assessment graph) stays T-C06's F-168 mapping territory — corpus-blocked on past papers, seeded by the 11 SME topic-question pages; the 4CH0 fixture is hard-separated (no 4CH0→4CH1 mapping, checked loudly). **PILOT EXECUTED 2026-09-11 (session 38, per the operator's design-first tasking): architecture + pilot spec authored BEFORE any generation — `graph/reports/C11_ARCHITECTURE.md` (schemas/provenance/confidence-with-derivation-caps/validation states incl. REVIEW_REQUIRED/split-first identity/command-kind tagging/deterministic contract/4.15 negative-control rules/negative-test spec/V2 projection/§16 expansion criteria; §0 records the tasking audit — baseline verified, every divergence an omission resolved toward the frozen contracts, κ replaced by honest raw agreement). The 1.25–1.36 pilot graph: `scripts/c11_pilot_decisions.yaml` (AI decision record) → `c11_concept_pilot.py` (fail-closed, 12 gate families, byte-verified quotes, T-C10-mapping-resolved attachments, 4.15 structurally uncoverable, anti-forgery) → `graph/concepts.yaml` + `graph/concept_edges.yaml` + `graph/spec_command_kinds.yaml` = 29 nodes (27 CONCEPT + 2 MISCONCEPTION incl. 1 WRONG_ANSWER_PATTERN) / 66 edges (33 PART_OF + 26 REQUIRES_PREREQUISITE + 3 EXPLAINED_BY + 1 MISCONCEPTION_OF + 2 REMEDIATED_BY + 1 WRONG_ANSWER_PATTERN; 0 RELATED_TO / 0 COMMONLY_CONFUSED_WITH = honest abstention) / 64 SUGGESTED + 2 REVIEW_REQUIRED / **0 promoted**; 12 held/rejected candidates with named failing rules (incl. the 4.15 premise+consequence class REJECTED). Gates ALL GREEN: graph_check 11/11 (new c11 groups), `c11_negative_test.py` 14/14 (12 corruption classes + PC + idempotence; the 4.15 classes prove the negative control), deterministic regeneration byte-identical. Adversarial pass-2 + raw agreement (NOT κ): 29/29 nodes, 31/31 asserted edges confirmed, 2 uncertain edges concordantly non-asserted → operator decision; 3 FP findings + 3 FN concerns recorded. Reports: `C11_PILOT_REVIEW_SHEET.md` (THE OPERATOR GATE — per-row verdicts, settles the 2 REVIEW_REQUIRED edges, merges are operator-only) + `C11_PILOT_REVIEW.json` + `C11_PILOT_REPORT.md` + `C11_COVERAGE_GAP_REPORT.md` (4.15 = zero manufactured coverage, machine-enforced; boundary concepts + MS-mining breadth the expansion inputs). NEXT: operator reviews the pilot sheet → then (a) promotion batch tooling (c10_promote pattern) for the confirmed set, and/or (b) §16 authorization + scoped expansion plan for the full graph. No mass generation, no DB writes, no promotion until then.** **SESSION-43 ADDENDUM (2026-09-12, zero promotion authority): the operator review package is DELIVERED — `graph/reports/C11_REVIEW_PACKAGE.md`/`.json` + the operator-owned verdict template `scripts/c11_review_verdicts.yaml` + `scripts/c11_review_package_build.py` (deterministic, fail-closed asserts, 78/78 quotes independently byte-verified) + `scripts/c11_batch_forecast.py` → `graph/reports/C11_BATCH_FORECAST.json` (§16-model calibration vs pilot actuals; rates; six FP categories; future-batch schema). 31 edge dossiers (26 LIKELY_SAFE / 2 FC-3 OD-1-linked / 1 granularity / 2 PENDING) + 29 concept dossiers (27 safe / 2 FC-2 yield-merge-pair) + 13-entry held/rejected appendix (no reopening; HELD-08 taxonomy-gap finding) + OD-1/OD-2 PENDING-RATIFICATION slots + the 'maximum yield' alias audit (phrase = 0 corpus occurrences; 47-alias classification 25/4/18; alias NOT modified; disposition DROP/RE_EVIDENCE/KEEP/RENAME) + review reduction (60 rows → ~7 decisions + bulk groups). The phantom 'aximum yield' report text corrected in 3 artifacts + the renderer literal (the data was never corrupted — report/data drift). Nothing promoted; graph byte-untouched; §16 NOT opened. Awaiting: operator verdicts in c11_review_verdicts.yaml → the next session applies them (§18 promotion / §7 re-authoring); §16 authorization remains a separate explicit operator action.** **SESSION-44 ADDENDUM (2026-09-12, RECORDED-NOT-APPLIED): the operator verdict set is ENCODED — `scripts/c11_review_verdicts.yaml` filled VERBATIM by `scripts/c11_verdict_encode.py` (fail-closed pre-write reconciliation): OD-1 RATIFY (yield triple stays split — ACTUAL/THEORETICAL/PERCENTAGE as the generalized formula-driven-coverage ontology rule) + OD-2 RATIFY (incidental terminology / table labels NOT sufficient instructional evidence); edges 28 CONFIRM + 3 HOLD (E-08 granularity / E-26 EXPLAINED_BY→AVOGADRO-LAW / E-29 REMEDIATED_BY→CONSERVATION-MASS, operator rationale verbatim) + 0 REJECT; nodes 29 CONFIRM with enrichment notes on N-08 (Avogadro constant @1.27) and N-27 (Avogadro's Law @1.35C) scoped as enrichment, never syllabus authority; alias policy RETRIEVAL_EXEMPT (four-rule discipline: evidence-backed where supported; retrieval-only never corpus-evidenced; unevidenced stay marked; never edge evidence); 'maximum yield' KEEP retrieval-only/unevidenced (0 corpus occurrences; not in evidence quotes; source note unaltered; not data corruption); held appendix acknowledged — 13 preserved verbatim (11 HOLD + 2 REJECT). HELD-08 accounting fixed report-only (§19 + §16-report item 6 now 13 of 13; FP-6 → remediated; verdict unchanged). NEW GATE `scripts/c11_review_verdict_check.py` 37/37 (schema / store reconciliation / decision-set shape / zero-action / 5 note-row quotes byte-verified); `c11_review_package_build.py` hardened against clobbering the filled verdict record. FULL SUITE GREEN at the final state (graph_check / negative 14/14 / task4 3/3 / promote 27/27 / diff-review / verdict 37/37 / generator re-run sha256-identical). ZERO ACTION: 0 promoted, graph + decision record byte-identical, §16 NOT authorized. Next: a dedicated application session runs the 28 confirmed edges through the §18 promotion pathway and §7 re-authoring for alias marking / enrichment scoping — §16 authorization stays a separate explicit operator action.** **SESSION-45 ADDENDUM (2026-09-12, APPLICATION EXECUTED): the 28 CONFIRM edges are PROMOTED — pre-write reconciliation PASS (CONFIRM == actionable-clean == live SUGGESTED-minus-HOLDs, 28 each), the ratifying bundle regenerated (C11_DIFF_REVIEW_2026-09-12.md, 28 pending / 4 not-actionable), `c11_diff_review.py approve --all --by operator --date 2026-09-12` → ONE `c11_promote.py` invocation → `scripts/c11_promotions.yaml` = 28 entries (operator, 2026-09-12, review_reference = the bundle) → gated G13 re-run ALL GATES GREEN: live edges = **28 HUMAN_VALIDATED + 3 SUGGESTED semantic (the operator HOLDs, not promotable) + 33 PART_OF (derived, untouched) + 1 REVIEW_REQUIRED (RR edge)**; three-way set equality verified (live HV == verdict CONFIRM == store entries). §7 re-authoring applied (fail-closed c11_verdict_apply.py, audited + generator re-run byte-identical): E-08 HOLD block (new) + E-26/E-29 PENDING→HOLD (operator rationale verbatim); N-08/N-27 enrichment-scoping CONFIRM blocks (do-not-convert clause); N-14 'maximum yield' `aliases`→`retrieval_only_aliases` (RETRIEVAL_EXEMPT dual-track) + new G14/c11.14 one-track gates. Two front-end defects fixed with dated comments (held-substring hint now non-authored-only, was mis-flagging CON-EMP-MOL-CALC→CON-MOLE as HELD-06; consistency check now models the §18 transition). Test expectations updated ONLY in the promotion layer (verdict-check B2/B5/D1 applied-state, 35/35; R1 post-application surface; mut_07 re-targeted to a non-promoted SUGGESTED edge; promote-test restore → pre-promotion pristine sandbox). S16 gate report amended with dated notes (28 HV / 4 HOLD / 0 PENDING; blockers 1-3 DONE; **item 4 — the explicit §16 authorization — remains the SOLE blocker**). FULL SUITE GREEN (graph_check 29/65/28-HV/4.15-uncovered; verdict check application-reconciled; negative 14/14; promote 27/27; task4 3/3; diff-review ALL PASS; generator re-run byte-identical ×3). INVARIANTS: §16 NOT authorized; 4.15 uncovered; PART_OF outside §18; evidence/provenance verbatim; held appendix 13 untouched; no expansion, no DB writes. Resources 5715396 → 08f5ec9. NEXT: the operator's §16 decision (explicit authorization, or commission the scoped expansion plan) — node promotion (via PART_OF authority) remains deferred to the expansion round per §18 scope limits.** **SESSION-46 ADDENDUM (2026-09-12, §16 AUTHORIZATION RECORDED): the explicit §16 authorization is GIVEN — verbatim "Okay, I authorize" — recorded operator-owned in `scripts/c11_s16_authorization.yaml` (scope.unlocked = the item-14 batching shape: 14 batches / 4 phases / ≈12 SPs per batch, each with its own extraction_pass + decision record + gated generation + pass-2 adversarial review + review sheet + OPERATOR GATE, the consolidated cross-slice boundary ruling before phase 2, per-batch mark-scheme mining; scope.not_authorized = DB writes / V2 projection, any promotion without per-batch verdicts through §18, the frozen pilot dispositions (3 HOLDs + RR + held appendix 13 + 4.15 negative control), expansion beyond 4CH1, hand-edited graph, AI attribution) + validated by the new fail-closed gate `scripts/c11_s16_authorization_check.py` **22/22 PASS** (anti-forgery attribution; authorization-only payload — grants a gate, never identities; cross-file consistency; live invariants robust to future batch growth) — **negative-tested: 4 corruption classes all caught** (forged AI attribution, altered operator statement, injected forged promotion payload, stripped report markers). Governance reports amended with dated session-46 notes (gate report header/blockers item-4 DONE/closing; architecture §16; forecast regenerated deterministically — pilot promoted 0→28, promotion_rate 0.4308 (28/65), projection status = the sanctioned execution shape, no batch executed). ALL FOUR §16 blockers settled; operator gate list 14/14 DONE. Nothing generated, nothing promoted this round — the graph + frozen operator records byte-identical; full suite green (graph_check / verdict / negative 14/14 / promote 27/27 / task4 3/3 / diff-review / authorization 22/22 / generator re-run byte-identical ×3). Resources 08f5ec9 → 9ce37bc. NEXT: the operator commissions phase-1 batch 1 (S1 remainder, 1.1–1.24, ≈12 SPs → ≈30 nodes / ≈33 authored edges / ≈12 held per the forecast) — each batch = extraction_pass + decision record + gated generation + pass-2 review + review sheet + operator gate.** **SESSION-47 ADDENDUM (2026-09-12, BATCH 1 EXECUTED TO ITS OPERATOR GATE): phase-1 batch 1 commissioned ("run batch 1") and run through the full sanctioned pipeline — extraction_pass c11-s16-batch-1 (4CH1-1.1–1.12 + PR-01; 10 notes + 2 pinned MS) → `scripts/c11_batch1_decisions.yaml` (24 nodes / 29 authored edges [28 SUGGESTED + 1 subsumption-class RR self-quarantine] / 12 held; quotes byte-verified) → generator extended to the decision-record REGISTRY (fail-closed) → merged store 53/118 with the 28 promotions + frozen dispositions byte-intact → pass-2 review (zero demotions, flags enumerated) → `C11_BATCH1_REVIEW_SHEET.md` + the operator verdict template `scripts/c11_batch1_verdicts_template.yaml`. NOTHING promoted: the 28 batch-1 SUGGESTED edges are the pending §18 actionable surface. Full suite green; three §18 front-end defects fixed (dated); one pre-existing FP-5 drift documented (pilot sheet/json session-41-stale — next controlled revision). NEXT: operator verdicts → apply via §18/§7 → batch 2 (1.13–1.24).** **SESSION-48 ADDENDUM (2026-09-12, GATE SETTLED + APPLIED — the operator's ruling verbatim "CONFIRM all"): ENCODE `scripts/c11_verdict_encode_batch1.py` (fail-closed pre-state: template untouched, store reconciled 28+1 RR/24/12, pilot frozen 28-HV, §16 AUTHORIZED) → `scripts/c11_batch1_verdicts.yaml` (template filled + RENAMED per the gate pathway): 28 edge CONFIRM / 24 node CONFIRM (B1-N-08/B1-N-11 keep ENRICHMENT scoping — never syllabus authority) / 4× KEEP_AS_IS identity decisions (the authored representation confirmed: no merges, no splits, no symmetric-pair re-emission) / RR settlement HOLD_REVIEW_REQUIRED (the authored subsumption quarantine CONFIRMED — stays REVIEW_REQUIRED, un-promotable) / held appendix acknowledged (12, no reopening); ruling + mapping recorded verbatim in meta.operator_ruling. §7 re-authoring `scripts/c11_verdict_apply_batch1.py` (byte-exact anchors + authored-fields-identical guard): RR operator_decision HOLD_REVIEW_REQUIRED block + B1-N-08/B1-N-11 enrichment CONFIRM blocks (the pilot N-27/N-08 patterns) + session-48 header note in c11_batch1_decisions.yaml; generator re-run byte-identical. §18: bundle C11_DIFF_REVIEW_B1_2026-09-12.md (28 pending / 5 not-actionable; session-45 bundle preserved at its own path) → approve --all --by operator --date 2026-09-12 → ONE c11_promote.py invocation → 28 entries (STORE TOTAL 56, all operator) → gated G13 re-run: 56 HUMAN_VALIDATED in the live store (28 pilot + 28 batch-1), 3 pilot HOLDs + 2 RR un-promoted, batch-1 nodes SUGGESTED (no node §18 pathway). NEW STANDING GATE `scripts/c11_batch1_verdict_check.py` 33/33 — three-way set equality (verdict CONFIRM == live batch-1 HV == store batch entries) + pilot-slice permanence + 4.15 + no PART_OF/node promoted. Tooling fixes (dated): diff-review export --out relative-path resolution (crashed at the --review-ref render); review-build template re-emission guard (a recorded verdict round must never be shadowed by a fresh empty template). State-moved expectations only: pilot-verdict-check B5/D1 → pilot slice of the grown store; diff-review R1 → 0/5/56; graph_check C11_COUNTS comment + summary phrase; negative mut_07 dated note. Forecast: operator_verdicts confirm 28, promotion_rate 0.9655 (28/29), status SETTLED; gate report + architecture §16 amended with dated session-48 notes. FULL SUITE GREEN at the final state (incl. the new 33-check gate; generator/review-build/forecast byte-identical). Resources 50351e1 → 5dce457. NEXT: batch 2 (S1 remainder 1.13–1.24) commissionable — same per-batch shape; the consolidated cross-slice boundary ruling before phase 2 (S3).** **SESSION-49 ADDENDUM (2026-09-12, BATCH 2 EXECUTED TO ITS OPERATOR GATE): phase-1 batch 2 commissioned ("run batch 2") and run through the full sanctioned pipeline — extraction_pass c11-s16-batch-2 (4CH1-1.13–1.24 + PR-02; 7 notes + 6 pinned MS [ATOM1-3, PT, ECM1/ECM3 — the ECM pins close FN-B1-1]) → `scripts/c11_batch2_decisions.yaml` (14 nodes incl. 2 layout-verified mark-scheme misconceptions; 23 edges ALL SUGGESTED + ZERO RR + the first COMMONLY_CONFUSED_WITH edge + 5 sanctioned cross-boundary edges [no duplicate Ar mint — the pilot CON-AR owns the term]; 10 held; all 77 quotes pre-verified) → the generator registry extended to [pilot, batch1, batch2] → merged store 67/156 with the 56 promotions + frozen dispositions byte-intact → pass-2 review (zero demotions, flags enumerated) → `C11_BATCH2_REVIEW_SHEET.md` + the operator verdict template `scripts/c11_batch2_verdicts_template.yaml` (23 edge rows + 14 node rows + 4 identity decisions). NOTHING promoted: the 23 batch-2 SUGGESTED edges are the pending §18 actionable surface. Full suite green (batch-1 verdict check D6 re-anchored to slice preservation; R1 = 23/5/56). Forecast batch-2 record appended (nodes −51.4% = the boundary discipline). NEXT: operator verdicts → apply via §18/§7 → batch 3 (1.37–1.60C).** **SESSION-50 ADDENDUM (2026-09-12, BATCH-2 GATE SETTLED + APPLIED — the operator's ruling verbatim "CONFIRM all"): ENCODE `scripts/c11_verdict_encode_batch2.py` (fail-closed pre-state: template untouched, store reconciled 23 SUGGESTED + zero RR / 14 nodes / 10 held, live store frozen at 56 HV, §16 AUTHORIZED) → `scripts/c11_batch2_verdicts.yaml` (template filled + RENAMED per the gate pathway): 23 edge CONFIRM / 14 node CONFIRM (B2-N-08 CON-METALLOID keeps ENRICHMENT scoping) / 4× KEEP_AS_IS identity decisions (B2-ID-01: the CON-ISOTOPES/CON-AR boundary split stands — the pilot's CON-AR owns the Ar term, NO §7 re-scope; B2-ID-02/03/04: particle triple / periodic-table block / 1.20-1.21 classification stay single nodes) / held appendix acknowledged (10, no reopening) / NO RR settlement row (zero RR authored). §7 re-authoring `scripts/c11_verdict_apply_batch2.py` (byte-exact anchors, authored-fields-identical guard): the B2-N-08 enrichment-scoping operator_decision CONFIRM block + session-50 header note; generator re-run byte-identical (concepts.yaml + spec_command_kinds.yaml untouched). §18: bundle `C11_DIFF_REVIEW_B2_2026-09-12.md` (23 pending / 5 not-actionable; pilot + B1 bundles preserved byte-intact) → `c11_diff_review.py approve --all --by operator --date 2026-09-12 --review-ref the B2 bundle` → ONE `c11_promote.py` invocation → 23 entries appended → gated G13 re-run: **STORE TOTAL 79 HUMAN_VALIDATED (28 pilot + 28 batch-1 + 23 batch-2, ALL operator)**; the §18 actionable surface is empty again (R1 = 0/5/79). NEW standing gate `scripts/c11_batch2_verdict_check.py` ALL PASS (32 checks, three-way set equality per slice). State-moved expectations only (dated session-50 comments): batch-1 verdict check D4 56→79 (slice preserved exactly inside the grown store); diff-review R1; graph_check comment + summary phrase; negative mut_07 note; forecast batch-2 record SETTLED (confirm 23 / promotion_rate 1.0). Governance reports amended (S16 gate report + architecture §16, dated notes). Full 9-gate suite green; generator + review-build (re-issue guard held) + forecast byte-identical; git diff --check clean. INVARIANTS: frozen pilot + batch-1 dispositions byte-intact (E-08/E-26/E-29 SUGGESTED; both RR edges REVIEW_REQUIRED; HELD-01..13 + B1-H-01..12 + B2-H-01..10 untouched); batch-2 nodes SUGGESTED (no node §18 pathway); PART_OF outside §18; 4.15 uncovered; no DB writes; AI attribution forbidden. ENVIRONMENT: this session started from a sandbox rollback to the session-44 state — both repos fast-forwarded to the remote (b884cfb / a491f3b) recovering sessions 45-49 before any work; nothing re-authored from memory. Resources b884cfb → eeb6021 (rebased over the concurrent CI commit 8118d93, gates re-run green at the rebased state). NEXT: batch 3 (S1 remainder 1.37–1.60C) commissionable — same per-batch shape; the consolidated cross-slice boundary ruling before phase 2 (S3).** SESSION-51 ADDENDUM: batch 3 (the FULL S1 remainder 4CH1-1.37–1.60C + the Paper-2 MS pins, operator-commissioned by the move-forward directive) AUTHORED TO ITS OPERATOR GATE per the session-47/49 shape — FN-B2-1 closed (IONIC/COVALENT/CFEC_MS_P2 pinned; no metallic/electrolysis MS exists — recorded); 24 nodes / 39 SUGGESTED edges (zero RR; first RELATED_TO + second CCW; 6 cross-boundary edges into batch-2 nodes) / 14 held; 144/144 anchors pre-verified; merged store 91/220/97+123 with 79 HV unchanged; review sheet C11_BATCH3_REVIEW_SHEET.md + template c11_batch3_verdicts_template.yaml (39+24 rows + B3-ID-01..07); all 9 gates green; deterministic; frozen dispositions intact. NEXT: operator batch-3 verdicts → application session (§18 + §7); then batch 4 = S3 Physical (Section 1 coverage COMPLETE); consolidated cross-slice boundary ruling before phase 2. **SESSION-52 ADDENDUM (2026-09-13, GATE SETTLED + APPLIED + BATCH 4 STARTED — the operator's practical-review directive): ENCODE `scripts/c11_verdict_encode_batch3.py` (fail-closed: template untouched, store reconciled 39 SUGGESTED + zero RR / 24 nodes / 14 held, live store frozen at 79 HV, §16 AUTHORIZED) → the operator-owned `scripts/c11_batch3_verdicts.yaml` (template filled + RENAMED): 39 edge CONFIRM / 24 node CONFIRM / 7 identity decisions KEEP_AS_IS / 14 held acknowledged (clean quarantine — no rescue attempts) / no RR settlement row; the policy recorded verbatim in meta.operator_ruling ('Where the evidence clearly supports the authored relationship: CONFIRM... do not turn ordinary ontology imperfection, wording preferences, enrichment opportunities, or theoretical alternative interpretations into blockers'; FLAGGED pre-triage labels are NOT a reopening trigger; 'My verdict file is that operator authorization.'). §7 re-authoring header-note-only (`scripts/c11_verdict_apply_batch3.py` — no RR block, no ENRICHMENT node, no MERGE/SPLIT; zero operator_decision blocks; generator re-run byte-identical). §18: bundle C11_DIFF_REVIEW_B3_2026-09-13.md → approve --all --by operator --date 2026-09-13 → ONE c11_promote.py invocation → 39 promotions → **STORE 118 HUMAN_VALIDATED (28+28+23+39, all operator)**; R1 = 0/5/118. NEW standing gate `scripts/c11_batch3_verdict_check.py` 32/32 (three-way set equality per slice; no operator_decision blocks; §7 header note; slices intact). Batch 4 STARTED: cross-slice boundary ruling `scripts/c11_batch4_boundary_ruling.yaml` (machine-verified zero canonical conflicts — 20 S3 terms vs the whole S1 store; 5 sanctioned boundary-edge targets CON-MOLE/CON-COVALENT-BOND/CON-CONCENTRATION/CON-EQ-SYMBOL/CON-WATER-CRYST with exact ownership; boundary MINTING forbidden; non-goals: no ontology redesign/re-scope/promotion authority) + checker `scripts/c11_batch4_boundary_check.py` 20/20 + the S3 Paper-2 MS pins (ENERGETICS/RATES/RRE_MS_P2 — all three families covered, unlike batches 1-3) + 15 notes inventoried (22/22 SPs). Full 11-gate suite green; deterministic; frozen dispositions byte-intact; 4.15 uncovered; SECTION 1 FULLY SETTLED (60/60 SPs). NEXT: the batch-4 authoring session (extraction_pass c11-s16-batch-4 → decision record → registry → pass-2 → C11_BATCH4_REVIEW_SHEET + verdict template) under the ruling's standing constraint; per the operator's exit directive T-C11 no longer gates phase 2 / learner-facing KG behavior / teacher-side work / pilot readiness. Resources b688140 → 3663665 → bde08f2 (pushed).** **SESSION-53 ADDENDUM (2026-09-13, BATCH 4 AUTHORED TO ITS OPERATOR GATE — zero promotions, no invented verdicts): Section 3 — Physical Chemistry executed per the operator's session-53 directive ('Execute Batch 4 using the established Batch-1/2/3 machinery.' / 'The purpose of this session is to get Batch 4 authored cleanly to the operator gate.'): all 15 HUMAN_VALIDATED-mapped S3 notes read in full + the 3 pinned S3 Paper-2 MS mined (full-family MS coverage for the first time); every planned quote probed pre-authoring (c11_batch4_quote_probe.py) and 128/128 anchors pre-verified fail-closed BEFORE the registry grew (c11_batch4_preverify.py); decision record `scripts/c11_batch4_decisions.yaml` (extraction_pass c11-s16-batch-4): **22 nodes** (17 CONCEPT per the session-52 mint-ruling families — 7 energetics / 6 rates incl. the 3.14C ACTIVATION-ENERGY+REACTION-PROFILE dual-node pair / 4 reversibility+equilibria; know pairs as dual attachments 3.12+3.13, 3.19C+3.20C, 3.21C+3.22C; 3.8/3.15/3.16 owned by the practicals PR-09/10/11) + **5 mark-scheme-documented WRONG_ANSWER_PATTERN misconceptions** (MIS-CATALYST-PARTICLE-ENERGY — the 'MAX 1 if any reference to particles gaining energy or moving more quickly' cap in BOTH S3 Paper-2 MS; MIS-EQ-TEMP-EXO + MIS-EQ-PRESSURE-FEWER — the RRE reject/accept antonym pairs; MIS-ENTHALPY-UNIT-J — the 'Award 1 mark for 50 000' J-to-kJ rule; MIS-BOND-ENERGY-COUNT — the 'Deduct 1 mark for each mistake' bond-sum rule + examiner tip) and **35 authored edges** (24 REQUIRES_PREREQUISITE + 1 EXPLAINED_BY [RATE-FACTORS EXPLAINED_BY COLLISION-THEORY] + 5 WAP + 5 REMEDIATED_BY; ALL SUGGESTED, zero RR; **exactly 5 sanctioned cross-section boundary edges** — one per ruled S1 owner [CON-MOLE, CON-COVALENT-BOND, CON-CONCENTRATION, CON-EQ-SYMBOL, CON-WATER-CRYST], no re-minting, a considered sixth held as B4-H-14) / 14 held candidates (B4-H-01..14, incl. the four CLASSIC misconceptions refused for missing documentation — static equilibrium, consumed catalyst, catalyst-shifts-position, bond-breaking exo/endo swap — the session-53 Step-3 rule 'Do not infer a misconception merely because an incorrect answer is theoretically possible') / 22 command kinds. **Merged store: 113 nodes / 275 edges (117 PART_OF + 158 semantic) / 82 command kinds / 118 HUMAN_VALIDATED UNCHANGED (authoring promotes nothing) / 2 RR frozen / 4.15 uncovered**; concept-graph coverage 82/182 SPs (S1 60/60 + S3 22/22 at candidate level). Pass-2 adversarial review (c11_batch4_review_pass2.yaml): ZERO demotions (raw agreement nodes 100% / edges 35/35; findings FP-B4-1..5 + FN-B4-1..3 — FP-B4-1: the bond-energy→covalent-bond boundary row is the one medium-confidence row to eyeball; FP-B4-2: two medium-confidence misconception rows). OPERATOR GATE DELIVERED: C11_BATCH4_REVIEW_SHEET.md + C11_BATCH4_REVIEW.json + the OPERATOR-OWNED scripts/c11_batch4_verdicts_template.yaml (35 edge rows B4-E-01..35 + 17 node rows B4-N-01..17 + 5 misconception rows B4-M-01..05 + 6 identity decisions B4-ID-01..06 + held appendix acknowledgment; zero RR section). Forecast batch-4 record appended (52.8/60.5/22.0 vs actual 22/35/14; promotion 0.0 pending the gate). State-anchored expectations re-anchored with dated session-53 comments only (graph_check C11_*; batch-2/3 verdict-check live-shape rows 91/220/97→113/275/117; diff-review R1 35/5/118 + sandbox registry member; boundary-check B/D blocks — the zero-conflict audit re-verified against the PRE-batch-4 store state) — no test weakened. Governance reports amended (S16 gate report session-53 update + state rows; architecture §16 session-53 amendment). FULL GATE SUITE GREEN at the authored-to-gate state (graph_check / pilot + batch-1/2/3 verdict checks / boundary check / s16 authorization 22/22 / negative 14/14 / promote 27/27 / task4 3/3 / diff-review); generator + review-build + forecast re-runs sha256-byte-identical; frozen pilot + batch-1/2/3 dispositions byte-intact (118 promotions all operator; the 14 batch-3 held candidates stay quarantined). Resources bde08f2 → b2c880a (pushed). NEXT: the operator's batch-4 verdict session (fill + rename scripts/c11_batch4_verdicts_template.yaml → c11_batch4_verdicts.yaml; sheet C11_BATCH4_REVIEW_SHEET.md) → the next session encodes + applies via §18/§7; then batch 5 = S2 Inorganic per the §16 phase order (its own cross-slice ruling: S1↔S2 + S3↔S2). Then STOP — batch 4 authoring → operator gate. Nothing more.** **SESSION-54 ADDENDUM (2026-09-13, BATCH-4 GATE SETTLED + APPLIED — Section 3 settled; batch 4 CLOSED): the operator's verdict directive ('verdicts → promotion → verification → continuation'; 'The operator verdict file itself is the authorization.') executed through the session-48/50/52 sanctioned shape. One GENUINE BLOCKING DEFECT found + fixed at the source before encoding could run: the session-53 review-build emission shipped an UNPARSEABLE verdict template (raw inner single quotes in a YAML single-quoted scalar, row B4-E-03; caught fail-closed at the encode step) — fixed in `scripts/c11_batch4_review_build.py` (YAML quote escaping at all three notes emissions + a new fail-closed post-write parseability check; exactly one template line changed, content byte-preserved; sheet + JSON re-verified byte-identical). ENCODE `scripts/c11_verdict_encode_batch4.py` (fail-closed: no double-fill; row shapes B4-E-01..35 / B4-N-01..17 + B4-M-01..05 / B4-ID-01..06; reconciliation 35 SUGGESTED + zero RR + 22 nodes + 14 held; live store frozen at 118 HV == the promotion store; §16 AUTHORIZED; all-operator attribution) → the OPERATOR-OWNED `scripts/c11_batch4_verdicts.yaml` (template filled + RENAMED per the gate pathway): 35 edge CONFIRM / 22 node CONFIRM / 6 identity decisions KEEP_AS_IS / 14 held acknowledged (quarantined; the four CLASSIC misconceptions B4-H-08..11 revisit only with new MS evidence; the unaudited sixth boundary edge B4-H-14 stays outside the five-target ruling) / NO RR settlement row; the §2 practical verdict policy recorded verbatim in meta.operator_ruling with the two §3 special-attention rulings, evidence actually inspected: FP-B4-1 (the bond-energy→covalent-bond boundary row) CONFIRM — the note's own dependency statement + the all-covalent worked-example set + the displayed-formula tip + the session-52 ruling; FP-B4-2 (the two medium-confidence misconception rows) CONFIRM — the MS per-mistake deduction + tip naming (MIS-BOND-ENERGY-COUNT) and the byte-verified REJECT entry with antonym-pairing attribution (MIS-EQ-PRESSURE-FEWER) are both documented wrong-answer patterns. §18 APPLICATION: bundle `C11_DIFF_REVIEW_B4_2026-09-13.md` (35 pending / 5 not-actionable) → dry-run verified → `approve --all --by operator --date 2026-09-13 --review-ref the B4 bundle` → ONE `c11_promote.py` invocation → 35 promotions → gated G13 re-run ALL GATES GREEN: STORE TOTAL 153 HUMAN_VALIDATED (28+28+23+39+35, all operator, 35 on 2026-09-13, review_reference = the B4 bundle). NO §7 re-authoring sanctioned (all identity KEEP_AS_IS, zero RR, zero ENRICHMENT/MERGE/SPLIT — the authored record byte-untouched). NEW standing gate `scripts/c11_batch4_verdict_check.py` ALL PASS (35 checks: schema incl. NO rr_settlement + the verbatim policy; verdict shape + record reconciliation + NO operator_decision blocks; three-way set equality verdict CONFIRM == live batch-4 HV == store batch-4 entries; C6 the five sanctioned boundary edges HUMAN_VALIDATED; slices intact 28/28/23/39; both RR edges REVIEW_REQUIRED; store 153; 4.15 uncovered; no PART_OF or node promoted; 113/275/117/158; D13 zero ruled S1 owner re-minted; D14 the 14 held quarantined). State-moved expectations only (dated session-54 comments): graph_check state comment + summary phrase (153 HV); batch-1 D4 + batch-2 D6 + batch-3 D7 118→153; diff-review R1 35/5/118→0/5/153; boundary-check D3 118→153; negative mut_07 dated note; forecast batch-4 record SETTLED (confirm 35 / promotion_rate 1.0 / status; JSON regenerated deterministically). Governance reports amended with dated session-54 notes (S16 gate report state rows + promoted-count 153 + SECTION 3 SETTLED closing; architecture §16 amendment incl. 'Batch 4 is CLOSED — do not reopen'). Determinism re-proven (generator + review-build [template re-issue guard held] + forecast re-runs sha256-byte-identical); git diff --check clean. FULL SUITE GREEN at the final post-promotion state; invariants held (frozen pilot + batch-1/2/3 dispositions byte-intact; 63 held quarantined; nodes stay SUGGESTED; PART_OF outside §18; 4.15 uncovered; AI attribution forbidden; graph/*.yaml never hand-edited). Operator copies under `download/t-c11-batch4-verdicts/` (verdict file + B4 bundle + sheet + SHA256SUMS). Resources b2c880a → 48b7c08 (pushed). NEXT per the operator's §8 directive: NO new T-C11 review loop — T-C11 is not a permanent gate on Phase 2 / learner-facing KG behavior / misconception-aware recommendations / teacher-side KG work / pilot readiness; the broader SyllabAI roadmap continues from the verified 82-SP settled state, with the next T-C11 slice (batch 5 = S2 Inorganic, cross-slice ruling S1↔S2 + S3↔S2) proceeding alongside.** **[2026-09-13 session-55 addendum — T-C11 GRAPH WIRED INTO THE LEARNER LOOP (the operator's next-step directive: 'turn the graph work into observable product behavior')]: the settled store now ships inside syllabai-core as byte-verbatim SHA-256-pinned classpath snapshots (`src/main/resources/concept-graph/`: concept_edges + concepts + practicals.yaml, cmp-verified against the resources repo) feeding an immutable `ConceptDependencyGraph` — HUMAN_VALIDATED-only by construction (the 3 frozen pilot HOLDs + 2 RR edges cannot enter), PART_OF dropped, endpoints fail-closed. `NextBestActionService` extended to `nba-rules/v1.1` with two deterministic graph-aware stages (T2b validated prerequisite chains → REVIEW_PREREQUISITE/VALIDATED_PREREQUISITE_CHAIN; T4b validated REMEDIATED_BY → REMEDIATE_MISCONCEPTION/MISCONCEPTION_REMEDIATION), joined to the KG by node code inside the subject subtree, gated by deterministic learner evidence, capped/deduped like every tier; no applicable edge ⇒ existing behavior byte-identical (Case C). Frontend 'Fix misconception' action. 304/304 unit tests green (+27), `ConceptGraphRemediationFlowIT` for CI; RECOMMENDATION_SYSTEM_ARCHITECTURE.md §6 dated amendment. No new review loop opened; T-C11 stays closed; snapshot upgrades are conscious hash-pinned changes (a future 4CH1 KG seed activates the stages against real curriculum rows automatically via the code join).** **Session-56 addendum (2026-09-13): that 4CH1 KG seed is DELIVERED as the teacher-side activation layer — `ConceptGraphSeedService` + six-file pinned `ConceptGraphSnapshotLoader` + V15 additive migration (CONCEPT node type; WRONG_ANSWER_PATTERN/COMMONLY_CONFUSED_WITH relations) project the official curriculum substrate + the settled graph into the existing Postgres KG (deterministic, idempotent, frozen-five excluded, canonical identity by code); teacher surface = POST /api/v1/teacher/concept-graph/activate + GET …/edges + the tree read model + the ConceptGraphView frontend tab; the learner NBA stages activate against the seeded rows automatically (IT-proven). T-C11 stays closed (batch 5 remains out-of-gate, alongside the roadmap).** **DB-reality addendum (2026-09-17, reconciliation pass — Neon production, read-only): the DB projection is in EXACT SYNC with the settled store** — knowledge_nodes carries 98 CONCEPT (all SUGGESTED — the nodes-stay-SUGGESTED invariant holds in production) + 19 MISCONCEPTION (15 SUGGESTED + 4 UNVALIDATED held), and the non-structural VALIDATED edge total is exactly 153 == the store's 153 HUMAN_VALIDATED (112 REQUIRES_PREREQUISITE + 14 REMEDIATED_BY + 13 WRONG_ANSWER_PATTERN + 9 EXPLAINED_BY + 2 COMMONLY_CONFUSED_WITH + 2 MISCONCEPTION_OF + 1 RELATED_TO); the 7 UNVALIDATED REQUIRES_PREREQUISITE + 4 UNVALIDATED MISCONCEPTION_OF match the held/quarantined population and cannot serve (HUMAN_VALIDATED-only by construction); structural PART_OF (236 VALIDATED + 117 SUGGESTED) is the Phase-1/2 spec skeleton outside §18. Remaining deltas: batch 5 (S2) still unbuilt (row stays open); hygiene — 92 ING-* TOPIC nodes (UNVALIDATED paper-scrape residue; 95 observed 09-16) sit in knowledge_nodes outside the 4CH1 serving path (retriever skips UNVALIDATED). Session-57's teacher+learner PILOT-VERIFIED verdict re-confirmed against today's DB state; the 182/182 VALIDATED 4CH1 spec points serve as SUBTOPIC-typed nodes (SPEC_POINT enum = T-C06 territory, absent).
- [x] T-C11 **session-57 (pilot-readiness) addendum**: the settled graph is now PILOT-VERIFIED end-to-end — teacher path (activate → 4CH1 → SP 4CH1-3.7C → concepts + validated relationships + folded misconception, honest empty states outside S1/S3, idempotent re-runs) and learner path (validated prerequisite + misconception-remediation NBA actions from real BKT/BDT evidence on the seeded codes) both proven over real HTTP + browser. Three shipped defects fixed by the verification itself (core 5991187 + f7adea7, web ca01d9e — the 124-vs-153 teacher edge read model, the subject-scoping of practice, the subject selector; CI green restored on both repos). T-C11 exit condition RE-CONFIRMED under the pilot lens: not a gate for the controlled pilot. Batch 5 (S2) remains next T-C11 work, alongside — not before — pilot operation.
- [ ] T-C12 (**RATIFIED + EXECUTED session 42 (renumbered from 41 — the concurrent T-C11 stream claimed 41), 2026-09-11** — owner decisions D1–D5 recorded 2026-09-11, plan §8; execution gated per plan §8.1, Gate A opened by operator waiver "Proceed, I approve" recorded in `BACKFILL-REPORT.md` §0) PMT bulk-download backfill of `syllabai-pastpapers` per its `PMT-BULK-DOWNLOAD-PLAN.md`: charter §13 20-step workflow at bulk scale (download ledger → throttled fetch ≥3 s + jitter → PDF verify + identity spot-check → normalize §24 → per-subject batch commits via git CLI); **EXECUTED: P0 CLOSED (4CH1+4CH0: PMT adds zero new artifacts — 20/20 planned rows SHA-256-proven duplicates of corpus per §19; gaps recorded: 2024-01/2025-01/2025-06/2019-01 not listed by PMT; 2 baseline ratification flags raised: corpus `2020-06/4CH1-2C/qp.pdf` prints `4CH1/2CR`, PMT's June-2020-R MS links are November-2020 documents) + P1 PLACED (170 paper dirs / 340 PDFs / 170 manifests under `international-a-level/chemistry/` after the session-45 audit repairs: WCH11–16 Jan 2019→Jan 2025 + WCH01–06 2009/10→2019-06, Oct/Nov sessions per D3; identities PDF-print-verified per §29; 79 GCE 6CH0x files quarantined out-of-scope-gce, 37 unresolved-identity with REASON.txt, 21 honest singletons; pages of record via z-ai page_reader (www PMT is Cloudflare-challenged), PDFs direct from pmt host); REMAINING: P2 IGCSE physics/maths sweeps → P3 IAL physics/maths → P4 biology → P5 stretch + optional provenance backfill of the 2,512 `source_url: null` baseline files (pattern now proven in session-42 manifests)**; **AUDITED & REPAIRED session 45 (2026-09-12)** — full subject-folder audit of `syllabai-pastpapers` (its `docs/AUDIT-2026-09-12.md`): corpus structurally clean (0 manifest↔dir field mismatches, all sha256/size valid, quarantine 313/313 REASON-paired, D2/D3 honored); two content findings repaired with print verification (commits 4a7192ed + 1e3e4b36): **F1** `wma01/2014-01/WME02-01/qp.pdf` had held the Core-Maths-C1 6663A paper (partner-inference filename collision) → true WME02/01 Mechanics-M2 Jan-2014 QP restored from `_quarantine/duplicate-artifact/` — the pipeline had quarantined the RIGHT twin; **F2** `wch11/2019-01/WCH11-01` had been committed manifest-less (169-vs-170 discrepancy) → completed: manifest written, ms print-verified, qp re-fetched from the ledger-recorded PMT URL and print-verified, ledger rows → normalized; **F4** doc-sync (INGESTION_REPORT covers both batches, plan/README execution status, BACKFILL-REPORT §7 addendum); OPEN operator decisions from the audit: F3 6663A international variant labelled under gce-a-level (34 confidence-5 date_rule manifests to spot-check during ratification), F5 specimen subtree (42 refs) vs charter §12, F6 month-`11` sessions (18 dirs) vs D3 `-10` normalization, F7 227 file-gap dirs (all 40 4CH1/4PH1 R-variant dirs MS-only) to be loaded as P2/P3 ledger rows, F8 minor (insert.pdf ×18 not in §24 list; 12 wch specs lack specification.yaml); priorities originally P0 complete IGCSE Chemistry 4CH1 series (ADR-019 pilot subject) → P1 IAL Chemistry WCH01–06 + WCH11–16 (zero coverage → covered) → P2–P5; binding decisions: D1 repo stays public (standing takedown policy) · D2 QP/MS only — ER sweep deferred · D3 Oct/Nov IAL sessions fetched wherever PMT lists them · D4 strict sequencing — P0 must CLOSE before P1 opens (Gate B — honored, recorded in BACKFILL-REPORT §4) · D5 ratification first — no PDF fetch until the operator's human-validation pass over the 1,361 `AI-IDENTIFIED` baseline dirs or a written waiver (waived 2026-09-11); politeness throttle + UA, `_staging/` never committed, PMT recorded honestly as third-party archive (charter §13 tier 5) with per-file page-of-record URLs; corpus-only work — no `syllabai-core`/platform ingestion (T-C06/T-C07 gates still apply), no learner serving from the repo.
- [ ] T-C04 T-C03 human content validation + batch-size decision — **DEFERRED / READY TO RESUME (session 13, 2026-09-06): intentionally deferred while product-facing implementation proceeds (T-025 → T-028 → T-029). NOT blocked — nothing is failing; T-C03 is complete and merged and the validation package is ready. Not cancelled, not obsolete, not complete. No expansion of GLM-OCR batch size and no learner serving of unvalidated GLM-OCR content is authorized until T-C04 validation is completed.** The gate when it resumes: a human reviewer answers "does the system preserve assessment content accurately enough to safely increase batch size?" — if NO: fix parser/bridge first; if YES: propose the next controlled batch size (still bounded, still one batch at a time). **Resume prerequisites, in order: (1) review the controlled 3-pair corpus; (2) review June representative content (e.g. Q1 MCQ + Q18 structured, per the package); (3) review the October Q18 conflict (part-marks sum 2 vs printed total 8, preserved verbatim); (4) review the October 1A 80-vs-120 paper-total conflict (REVIEW_REQUIRED, neither side wins); (5) verify question/part/mark-point preservation against the source PDFs / GLM-OCR Markdown (`SyllabAI/Past-Papers/GLM-markdown-sample`); (6) verify figures/diagrams and image references (signed URLs expired 2026-04-07 — known corpus limitation, preserved as `availability: unavailable-signed-url`); (7) decide whether extraction fidelity is sufficient to increase batch size; (8) only after validation, decide the next controlled batch size; (9) re-run audit/invariants after any expansion.** Validation package: `download/t-c03-human-review-package.md` + `download/t-c03-batch-audit-report.json` (canonical copies: the CI workflow artifact of run 34015454011 and the batch-root copy written by any ops run) — includes DB counts evidence and the content status lifecycle (SUGGESTED → teacher VALIDATED → servable; never automatic). Historical T-C03 evidence is immutable — do not rewrite it. Dependencies: T-C03 merged (met). **Session-60 status (2026-09-13): T-C04 r2 CORPUS INGESTION CAMPAIGN COMPLETE (the operator's sanctioned next step, executed after the sandbox lost the entire campaign environment — rebuilt from scratch: source-compiled PostgreSQL 17.11+pgvector, sparse-cloned Past-Papers c42b6a14, parser 5b92886, core b37837a): 81/82 sessions ingested across 17 batches, every batch green on all five invariants + cross-transaction reruns + cumulative DB gates; 758 SUGGESTED question versions (0 learner-servable — proven over the real API; 0 embedded; the 8 seed VALIDATED untouched); 1c-2016jan quarantined for operator/original-PDF resolution (AGENT.md rule 5); 34 REVIEW_REQUIRED bridge records preserved verbatim; verify_final_state 32 PASS / 0 FAIL; durable exports at download/ingestion-campaign-r2/ (milestone-batch-001 + milestone-final + cumulative-audit-report + SHA256SUMS). The reproduction exactly matches the operator-side r2 baseline contract (81/82, 17 batches, 1507/1507 figures). REMAINING for T-C04: the HUMAN VALIDATION stage — the r3-5 gated teacher-decision importer (core b37837a, V18) is the sanctioned SUGGESTED→VALIDATED path; nothing may serve until validated/approved; 4CH1 learner questions honestly remain 0.** **Session-61 status (2026-09-13): TEACHER-VALIDATION TRANCHE-1 COMPLETE through the existing r3-5 importer — the validation pipeline is PROVEN and ready to scale. 14 representative versions (Specimen 2017 both papers, January 2021, a figure item, a REVIEW_REQUIRED-bridge item, an old-spec 4CH0 item) reviewed with machine-verified extraction fidelity; 11 VALIDATE + 2 FLAG + 1 REJECT applied atomically (run 65f497aa), then idempotency re-proven (no-op) and one below-bar promotion REVERSED with audit (run ae326665) — final 10 promoted / 1 REJECTED / 747 SUGGESTED / 15 audit events, seeds untouched, contamination 0, evidence tables byte-identical. Negative tests 7/7 fail-closed. Learner-serving boundary verified over the real API (16/16): only validated content serves; WCH11 stays 8; STRUCTURED marking is the Smart-Mark/human queue by design. Durable export: download/teacher-validation/tranche-1-final/. NOTE: the promoted items are pipeline-validation decisions under the operator's directive (reviewer recorded honestly as session61-operator-directive, per-item dossier + REVERSE available) — NOT final pedagogical validation, and NOT a declaration that the corpus is learner-ready; subject-surface serving additionally needs KG mapping (T-C06/F-168). NEXT for T-C04: operator decides the tranche-2 size (recommend ~40–60 versions prioritizing scheme-linkage repair + Specimen/Jan-quality sessions first); monitor the scheduled runs for the cron-delay record.** **Session-62 status (2026-09-13/14): BATCH-2 COMPLETE (~40 candidates, operator's mark-scheme-quality directive) — the validation system held the evidence line as the sample tripled. Baseline hash-verified vs the Session-61 durable dump before any change; repos fast-forwarded to the operator's same-day importer hardening (core 9747d0a F-1/F-2/F-3, CI-green — Batch-2 ran through the HARDENED importer, no mechanism change). 40 diverse candidates (Specimen/Jan-2020–2023/Nov/Summer + deliberately-retained boundary cases: RR-bridge x3, thin-scheme, marks-field=1, stem-only); 3-layer mark-scheme census (MS.md source vs canonical vs extracted) classified every candidate A/B/C/D/E — dominant finding: canonicalization drops continuation rows ('(ii)'/'(iii)') so DB schemes lose content the MS.md sources contain. Decisions: 10 VALIDATE / 30 FLAG (annotations only — still SUGGESTED) / 0 new REJECT (nothing content-corrupt). Apply run 230ccc50 atomic: 747→737 SUGGESTED / 10→20 VALIDATED / 15→55 events, promoted-set match, evidence tables byte-identical, seeds untouched, KG 96/21, RR 34; idempotent replay no-op; negative tests 9/9 with 0 unintended writes (2 F-1 cases uncaught-but-fail-closed — known observation, out of scope); learner-serving boundary 20/20 (unscoped 28 = 8 seeds + 20 promoted; FLAG-annotated never serve; WCH11 8 unchanged; GEN no KG root — validation alone does not map to a subject surface, T-C06/F-168 NOT implemented; structured marking stays PENDING by design; MCQ regression green). Monitor: dispatch 34778500876 = 14/14 green, teacher checks ACTIVE; both crons unchanged; 0 scheduled runs ever (GitHub cron delay recorded). Durable export: download/teacher-validation/batch2-final/ (28 files, SHA256SUMS-verified; tranche-1 export re-verified intact). NEXT for T-C04: operator decides Batch-3; recommended FIRST repair the canonical continuation-row loss (B-class) via canonical re-extraction — recovers the most FLAGged items without touching MS.md sources — then re-review.** **Session-63 status (2026-09-14): CONTINUATION-ROW REPAIR COMPLETE (the Batch-2 recommended first step) — the canonical-document hypothesis was refined by three-layer evidence: the canonical documents RETAIN every continuation row; the loss occurs in the mark-scheme DRAFT extraction (GlmOcrMarkSchemeExtractor row-prefix parsing: bare sub-part labels (ii)/(iii)/(b)(i)/(c) and two-level label rows '7 | (a)(i)' fell into the rowspan-continuation path where answers became guidance of the previous entry and marks cells were dropped). Fixed at the correct layer (parser 8949886 on codex/t-c03-batch: continuation-label rows open sub-part entries — bare roman continues the part letter, no letter context stays conservative; two-level label rows open compound entries; GROUP rows and multi-roman labels stay conservative by design). Regression 113/113 (9 new tests incl. the Specimen 2017 anchor frozen at 63 entries); determinism byte-identical on a full 81-paper double regen with 162/162 document ids + checksums identical to the campaign DB; impact measured before regeneration: 66/81 ms-drafts changed, 1408 entries / 3163 marks recovered, canonical/qp/reconciliation 0-diff, Specimen unchanged; no-invention/no-loss machine-verified 10/10 cases. Campaign DB UNTOUCHED (20/1/737 + 8 seeds / 55 events); 1 VALIDATED item (Jan-2021 q03) documented — canonical hash identical, decision's thin-linkage caveat recorded at the time, re-review recommended not required; 24 FLAG items' own questions recovered (all still SUGGESTED). Durable dossier: download/teacher-validation/canonical-repair/ (18 files); Session-61/62 exports intact. STOPPED — no Batch 3. NEXT: operator decides (a) whether to re-review the 24 enriched FLAG items before Batch 3, (b) whether/when to re-ingest drafts through the sanctioned importer in a separate explicit task. **Session-64 status (2026-09-14): REPAIR-ACCEPTANCE PASS COMPLETE — the repaired extraction layer is governance-safe and Batch 3 is UNBLOCKED at the technical level. Baseline locked read-only (DB 737/20+8/1, 55 events, 3 importer runs, KG 96/21, bridges 34+47, exports 21/28/18 intact; the Session-63 pushes had never reached origin — 8949886 + tracker fast-forward pushed, Drive-Sync CI green both). 365a817d re-review = OUTCOME A KEEP/CONFIRM (canonical hash + checksums identical, DB untouched, restored 3(b)(i)/3(d) verbatim from source, original thin-linkage caveat is what the repair mitigates; state unchanged, judgment recorded in the durable dossier not as a DB event). Both mark reallocations audited against source (2018jun 5(f) 1→5 restorative; 2014jan deferred-11 → 16(b) restorative); machine scans (last-match + multiset) over all 81 papers: exactly 2 reallocations + 3 marks-preserving relabels, ZERO unexplained changes. Permanent extractor contract committed (parser 408b8fb). Regression 9/9 + 113/113 + FULL conformance + no-invention 10/10 + Specimen frozen byte-identical. Third full regeneration 0-diff vs both determinism runs and the campaign archive (the 4 qp-draft diffs = documented operator paperReference overrides); 162/162 ids + checksums stable. Campaign DB proven equal to the Session-62 export state (34/34 tables content-identical — zero mutation). 24 FLAG items classified: 11 FLAG-EVIDENCE-REPAIRED (B01 B06 B09 B14 C03 C04 C05 C07 C10 + tranche-1 756d32a9/7555514a; caveats: C05 marks-field + C10/seq13 RR-bridge persists), 7 FLAG-REVIEW-WORTHY (B07 B10 B16 B17 B19 C01 C02), 6 FLAG-UNCHANGED-DECISION (B04 B05 B18 C06 C08 C09) — all still SUGGESTED, zero lifecycle changes. REMAINING for T-C04: operator decides Batch-3 (the 11+7 enriched FLAG items are the natural pool; the repaired drafts are staged at scripts/repair/regen-final — re-ingestion into the campaign DB is a separate explicit importer decision); optional operator PR codex/t-c03-batch → main for the remote parser-ci run on 8949886 (branch pushes don't trigger it by design; identical commands green locally). Durable dossier: download/teacher-validation/repair-acceptance/ (24 files). **Session-65 status (2026-09-14): BATCH-3 COMPLETE (46 candidates, operator's repaired-evidence directive) — executed after the workspace reset lost the local campaign state; the base was re-established deterministically (17/17 batches, 32/32 verify_final_state, counts == Session-64 baseline, db3058e7/eaf8b785 anchors reproduce) and the historical 61/62 promotion pools (Specimen-2017, 2021-Jan 1C, 2013-Jun 1C) EXCLUDED from candidacy with full disclosure. Selection: 46/716 with all-family spread — 42 REPAIRED-class, 19 RR-bridge, 37 figure + 37 equation bearing, 7 marks-field, 5 full-link, both audited-reallocation questions and both tranche-1 FLAG-analog classes included. Decisions: 4 VALIDATE (q02-27c8bd91 repair showcase; q03-37664a21; q07-3f246010; q09-8f519f1a) / 42 FLAG (14 marks-field=1, 11 divergent marks, RR discipline, 3 no-scheme, conservative residuals, QP-side part loss) / 0 REJECT. Importer 04f5972a atomic + idempotent replay no-op + 9/9 negative gates (zero unintended writes); after-state 754 SUGGESTED / 8 seed + 4 campaign VALIDATED / 46 events; KG 96/21 + RR 34 + evidence tables untouched; serving boundary 10/10 (FLAG never serve; WCH11 8; contamination 0; no 4CH1 activation; PENDING structured marking; MCQ path unchanged). Repair-impact: 3/42 repaired candidates now validatable; 39 blocked by non-repair defects; 0 continuation-row false positives; NEW defect classes: QP-side 'b)' paren-loss (1CR Jan-2022 q1 lost part b), no-letter-context-at-table-start short-answer loss (Jan-2019 q5 'H+'/'orange'), misplaced two-level relabel entry (Jun-2011 q1). Durable evidence: download/teacher-validation/batch3-final/ (88 files incl. the post-batch PG dump) + GitHub release t-c04-batch3-final. REMAINING for T-C04: operator decides Batch-4 scope; whether to re-ingest the repaired drafts (DB mark_points remain pre-repair — promotions are evidence-based on the repaired drafts); the three new defect classes for the next parser-hardening round; optionally an explicit operator-authorized re-application of the lost 61/62 decisions.**
  - **Session-66 (2026-09-14, autonomous policy):** extractor defect classes D1-D3 repaired at parser `2e61569` (124/124 tests; 312 entries / 586 marks / 2 QP parts recovered; Specimen frozen; canonical 0-diff; deterministic). Campaign DB RE-INGESTED at the fixed parser (verify_final_state 32/32; mark_points 2791) and the durable batch-3 layer re-applied (importer run `6b854cc4`). **BATCH-4 COMPLETE: 66 candidates → 22 VALIDATE / 44 FLAG / 0 REJECT** (importer run `02f1c458` atomic; 9/9 negative gates; idempotent; serving boundary 10/10). DB: 732 SUGGESTED / 34 VALIDATED (8 seed + 26 campaign) / 0 REJECTED / 112 events. Remaining blockers are source-level (RR bridges 22, marks divergences, mangled old-spec tables) — NOT extraction defects. Evidence: `download/teacher-validation/batch4/` (89 files) + `session66/`.

## Deferred (do not start — Cycle 2+)

DAT / teacher analytics depth · gamification · mock-exam blueprints & Mock Drop · spaced-repetition scheduler UI · PWA/offline/mobile · community features · multimodal input · handwriting OCR · podcast/voice · LTI/white-labeling · full recommender · MLE/EM BKT recalibration (stretch: Apache Commons Math).

## Program-management tasks

- [ ] P-001 Review `PROGRESS.md` + update `WORKLOG.md` after each substantial task (protocol in `AGENT.md`).
- [ ] P-002 Update backlog row status/owner as work lands (spreadsheet is definitive).
- [ ] P-003 Re-verify Groq/Gemini free-tier limits at build time (limits drift).
- [ ] P-004 Pin exact opendataloader-pdf version + record license check in worklog.
