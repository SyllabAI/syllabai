# TODO.md — SyllabAI Master Work Queue (Cycle 1 first)

**Scope guard:** only rows marked `Cycle 1` in `backlog/syllabai-master-project.xlsx` belong in the current execution window. The 12-spine critical path is F-020 → F-032 → F-033 → F-040 → F-041/F-043 → F-047 → F-055 → F-137/F-138 → F-148 → F-160.

## Wave 0 — foundations (this week)

- [x] T-001 Bootstrap `syllabai-core`: Spring Boot 4.1.x + Java 25 + Spring AI 2.0 skeleton, Maven, package structure per Master Spec §30. (F-002, F-008)
- [x] T-002 Flyway + Neon connection; pgvector extension enabled; dev profile with local Postgres via Docker Compose.
- [x] T-003 Spring Security + JWT auth skeleton; RBAC roles (STUDENT, TEACHER, ADMIN). (F-012, F-013)
- [x] T-004 REST conventions: `/api/v1` base, DTO boundaries, springdoc OpenAPI, error handling. (F-122)
- [x] T-005 `syllabai-web`: Next.js 16 + TypeScript + Tailwind scaffold on Vercel; API client typed from OpenAPI.
- [x] T-006 CI: GitHub Actions (build + test) on `syllabai-core` and `syllabai-web`.
- [x] T-007 Object storage adapter + Cloudflare R2 free bucket for past-paper PDFs. (F-021, F-149)

## Wave 1 — knowledge & content fabric

- [x] T-008 Canonical document format schema (JSON) + `DocumentParser` contract. (F-134, F-147) — syllabai-parser @ a43ad24, schema 1.0, 23/23 tests
- [x] T-009 In-process opendataloader-pdf integration (Maven) for text PDFs; bounding boxes retained. — opendataloader-pdf-core 2.5.7 (Apache-2.0, Central) 
- [x] T-010 Syllabus ingestion: Edexcel IAL Chemistry spec → curriculum tables → KG seed. (F-020, F-032) — parser `edexcel-numbered-outline-v1` (deterministic, per-node §17 provenance; real Pearson 2018 spec: 6 units / 20 topics / 15 subtopics) + core `CurriculumIngestionService` (idempotent, all-SUGGESTED) + teacher validation gate; prerequisites stay teacher-curated (never derived from an outline)
- [x] T-011 Past-paper + mark-scheme ingestion bridge: parser draft JSON → all-SUGGESTED content bank (paper/version/parts/scheme/points), teacher validation workflow, ingestion-anchor KG topic. Live-verified on real 4CH0/1C Jan 2012 (27 q / 62 parts / 33 points). Misconception/distractor links on structured items: Wave 3 (needs validated content). (F-028, F-029, F-152)
- [x] T-012 `KnowledgeGraphRepository` with recursive-CTE traversals; prerequisite queries. (F-032, F-135)
- [x] T-013 pgvector embedding pipeline (Gemini embeddings) over mark schemes/notes. (F-039, F-136) — core @ V11: canonical document store + deterministic chunking + Gemini text-embedding-004 (768-dim, `EmbeddingProvider` port, no failover by design) + HNSW cosine search; 121 unit tests + ContentPipelineIT

## Wave 2 — learner & assessment engine

- [x] T-014 `AssessmentEvidence` contract + attempt logging with timing, confidence, doubt flags. (F-053, F-055, F-056, F-153)
- [x] T-015 BKT engine with configurable params (L0=0.1, slip=0.1, guess=0.25, T=0.1) + parameter registry. (F-138)
- [x] T-016 Learner state aggregate: mastery, misconceptions, fluency (timed-vs-untimed gap, V8), confidence + self-doubt evidence. (F-137, F-033) — exam-literacy aggregate deferred to Wave 4 (needs marked structured data first)
- [x] T-017 BDT misconception engine driven by distractor evidence. (F-140)
- [x] T-018 Ebbinghaus decay nightly job (τ 30/90/365) + review-schedule feed. (F-159)
- [x] T-019 Timed vs untimed paired conditions + procedural_fluency_gap (V8 skill_states column, recomputed per node on evidence; null until paired). (F-162)
- [x] T-020 Learning-log telemetry: Paper B §3.5 fields, append-only event store. (F-160)
- [x] T-021 Smart Mark pipeline: candidate generation (LLM, pinned prompt v1) → deterministic bounds/coverage/mark-sum validation → append-only results; blank answers deterministic (no LLM). LLM never final truth. (F-047)
- [x] T-022 Human mark override + κ agreement gate: per-point paired Cohen's κ, threshold 0.60 recorded per evaluation, fail-closed gate; pre-gate smart marks provisional, post-gate authoritative; overrides never re-fire BKT. (F-144, F-161)

## Wave 3 — tutor & diagnostic intelligence

- [x] T-023 `LlmProvider` + free chain (Groq → Gemini → OpenRouter) with failover and rate tracking. (F-148, F-163)
- [x] T-024 KA-RAG orchestration: intent → KG context → hybrid retrieval → grounded generation. (F-040) — v0 foundation: deterministic intent (VALIDATED nodes only), KG+vector RRF fusion (rank-only, k=60), NoReranker Strategy, learner-state context assembly, grounded generation (prompt tutor-grounded/v1, free-LLM chain), citations with deep links, KA_RAG_COMPLETED telemetry (V12), deterministic refusal on empty evidence; POST /api/v1/tutor/ask (backend surface — chat UI stays T-025)
- [x] T-025 Tutor chat UI with verbatim citations + PDF deep-links. (F-041, F-043, F-022) — **COMPLETE and MERGED 2026-09-07 (explicit user instruction): web PR #1 merged as a fast-forward push (main `1685f4c` → `3402230`, no merge commit — linear history, main is exactly the CI-verified head; PR closed/merged, merge_commit_sha = `3402230`); post-merge CI run 34049500636 on main — Lint + type-checked production build both SUCCESS (step-level verified, not assumed).** Implementation (session 13, branch `codex/t-025-tutor-chat` @ `3402230`, PR CI run 34047801205 SUCCESS): TutorChatView — a new Tutor tab on the merged T-024 surface `POST /api/v1/tutor/ask` (synchronous v0, no SSE backend exists — loading/error/refusal states, no second backend architecture invented); `[n]` markers render as reference chips jumping to verbatim-source citation cards; matched-topic chips; refusal state distinct; traceability footer (model · provider · latency · evidence count, Spec §19); transcript survives tab switches; role-aware deep links (documented v0 decision); 2000-char limit mirroring backend `@Size`. Follow-ups that remain open on F-041/F-043 (tracked there, not blocking): browser verification of the chat flow against the live backend at pilot time; SSE/streaming when the backend ships it (Spec §22 future surface); in-app PDF deep-links stay F-022 (deliberately not started). T-026/T-027 diagnosis-aware policy integration still gated on unmerged core PR #1 (human decision, never automatic).
- [x] T-026 Tutor policy: diagnosis-aware intervention selection (Scientific-learning-skills prompts). — **COMPLETE and MERGED 2026-09-08 (explicit user directive this session: "merge/verify the existing work, finish the end-to-end adaptive student experience"): core PR #1 was rebased onto current main (T-033 already in) and its `V13__diagnosis_tutor_policy.sql` renumbered to **V14** — the old V13 collided with the already-merged `V13__glm_ocr_bridge_records.sql` (git-mergeable but Flyway would fail with duplicate version 13); no SQL content changed. Verification: 264/264 unit green locally on `04d6f10` (228 from main + 36 diagnosis tests); CI run 34151840343 SUCCESS on the rebased head incl. Docker ITs (Flyway V14 applies on a fresh schema); post-merge CI run 34152066031 on main SUCCESS. PR #1 marked ready (draft flag cleared via GraphQL) before the fast-forward merge; verification + authorization evidence posted to the PR. Main: `18e92ee` → `04d6f10`.** v0.2 semantics unchanged from the audited branch: precedence documented, teacher-override reads, struggle-type → intervention mapping with explainability (ADR-016 lineage).
- [x] T-027 Struggle inference v0: rule-based signals over learning log. (F-141) — **COMPLETE and MERGED with T-026 (core PR #1, see above): prereq-gap / 3b fluency / 5a self-doubt; supersede-keeps-history, 7-day expiry enforced on reads; unsupported types never emitted, tested.**
- [x] T-033 Next Best Learning Action v1 — the adaptive-loop closing surface (F-092 minimal Cycle-1 slice, promoted by the 2026-09-08 finish-the-product directive; ADR-017 deterministic baseline). — **COMPLETE and MERGED 2026-09-08 (explicit user directive: "merge/verify the existing work…"): the documented merge order was respected — core PR #8 FIRST (main `c7d22b9` → `18e92ee`, post-merge CI run 34151756702 SUCCESS), then web PR #4 (main `2c404a4` → `9e2931d`, post-merge CI run 34151763333 SUCCESS). Local re-verification before merging: core 228/228 unit on `18e92ee`; web lint clean + type-checked production build on `9e2931d`.** Backend: `recommendation` module — `NextBestActionService` (deterministic `nba-rules/v1`: rule tiers DUE_REVIEW → PREREQUISITE_WEAK → PROBLEM_QUESTION → MISCONCEPTION_SUSPECTED → FLUENCY_GAP → LOW_MASTERY → UNCOVERED_TOPIC with structured evidence-backed reason codes; one action per topic; capped portfolio; subject-subtree hard isolation; only servable question references) on `GET /api/v1/learners/me/recommendations?rootId=` (Spec §22); `ServableQuestionService` extracted as the single owner of the learner-serving boundary (QuestionController delegates, behavior identical — guarded by the existing ITs). Web: `NextBestActionsCard` on the dashboard (ranked advice with the backend's verbatim reasonDetail, deep-links to practice/tutor, honest empty/error states, distinct from the measured-fact cards). Scope guard: the full F-087…F-093 architecture (learned ranking, collaborative filtering, exploration machinery, exposure evaluation) stays Cycle 2+ — only this minimal slice is Cycle 1.
- [x] T-034 Attempt history + tutor handoff — the Review Hub minimal slice (F-059 attempt-history leg, charter §14; closes the missing loop leg of the end-to-end adaptive student experience). — **COMPLETE and MERGED 2026-09-08 (same session directive): core PR #9 (head `b71703b`, CI run 34152799603 SUCCESS incl. the new AttemptHistoryFlowIT with real Postgres; local 270/270 unit) → web PR #5 (head `99194af`, CI run 34152809194 SUCCESS — lint + type-checked production build). Merged fast-forward in dependency order: core main `04d6f10` → `b71703b`, then web main `9e2931d` → `99194af`; merge evidence posted to both PRs.** Backend: `AttemptHistoryService` + `AttemptHistoryController` — `GET /api/v1/learners/me/attempts?limit=` (default 50, max 100; identity from the JWT): strictly a read-only view over the immutable attempts/answers evidence rows — NO parallel tracking, NO derived mastery; honesty rules pinned by tests (structured attempts → null marks while any part is pending; attempt-level marks = sum of authoritative part marks only; MCQ rows carry the already-revealed chosen/correct option labels + the distractor misconception; evidenceEmitted rides along; topic identity via the shared KG, null-safe). Web: new **History** tab (`HistoryView`) — outcome/marks/marking-state/topic/timing/calibration per attempt, "Practise topic" deep-link closing evidence → review → retry, honest empty/error/awaiting-marks states; **Ask tutor about this** on a wrong MCQ result pre-fills an editable tutor question (never auto-sends) closing the weakness → tutor leg; history refreshes after every submission. Deliberately out of scope (stays F-059 Cycle 2+): problematic-questions aggregation, filters, due-review cross-linking — only the attempt-history leg is Cycle 1.
- [x] T-028 Mastery map UI (KG visualizer, simplified 2D first) + student dashboard. (F-036, F-060, F-034) — **COMPLETE and MERGED 2026-09-07 (session 15, explicit user authorization "OKAY PROCEED" on the session-14 pending merge decisions). The documented merge order was respected: core PR #6 FIRST, then web PR #2 — both merged as fast-forward pushes (linear history, no merge commit; each main is exactly the CI-verified head, GitHub marked both PRs closed/merged with merge_commit_sha = the pushed head). Pre-merge verification (API, not worklog): both PRs open + mergeable_state clean on the documented heads/bases, PR CI runs 34050895468 (core) and 34051246049 (web) completed/success; core PR #1 (T-026/T-027) verified open/draft/unmerged before AND after — the authorization touched only PR #6 and PR #2. Post-merge verification (observed, step-level): core main `1976540` → `ab7a52b`, push CI run 34053354490 on main — BUILD SUCCESS (Build and test, Upload test reports steps green; 204/204 unit + 22/22 IT incl. GlmOcrBatchIT 5/5 per the PR-run log inspection); web main `3402230` → `18543d2`, push CI run 34053451121 on main — Lint ✓, Production build (type-checked) ✓. Follow-ups that remain open (tracked on F-034/F-036/F-060, not blocking): browser verification of the dashboard/graph flows against the live backend at pilot time (same deferred-verification posture as T-025); the deeper F-036 polish (pan/zoom, 3D) stays out of Cycle-1 scope. Implementation history (session 14, branches):**
  - **Core PR #6 — MERGED** (fast-forward `1976540` → `ab7a52b`; PR closed/merged, merge_commit_sha `ab7a52b`; post-merge CI 34053354490 SUCCESS): F-034 `GET /api/v1/learners/me/knowledge-graph?rootId=...` (Spec §22) — `LearnerKnowledgeGraphService` composes the VERIFIED read services (`KnowledgeGraphService.treeWithMisconceptions` + BKT skill states with real `EbbinghausDecayService` decay + BDT misconception states + earliest pending review per node; no new persistence/traversal/mutation); `KnowledgeGraphService.prerequisiteRelations` (one batched `findPrerequisiteEdgesWithin` query over the recursive-CTE subtree); `DecayParams.bandOf` (band vocabulary beside its thresholds — `LearnerStateController` delegates, so /state and /knowledge-graph can never disagree). Honesty rules pinned by tests: unpractised nodes render nulls (never zeros/fabricated bands); misconception annotations only on MISCONCEPTION nodes; reviewDueAt = earliest PENDING review; `asOf` pins the decay instant. Local `mvn test` (JDK 25.0.4.1 + Maven 3.9.9, tools staged in the workspace): **204/204** (196 existing + 8 new). **PR CI run 34050895468 SUCCESS — step-level verified (mvn verify: 204/204 unit + 22/22 IT incl. GlmOcrBatchIT 5/5 regression proofs + KaRagFlowIT 3/3; log-downloaded and inspected, 0 failures/errors/skipped, BUILD SUCCESS).**
  - **Web PR #2 — MERGED** (fast-forward `3402230` → `18543d2`, after core PR #6 per the runtime dependency; PR closed/merged, merge_commit_sha `18543d2`; post-merge CI 34053451121 SUCCESS — Lint ✓, Production build type-checked ✓): `DashboardView` (F-060) — mastery summary (band chips + avg effective mastery + as-of), due reviews (overdue first + Practise CTA), focus areas (lowest MEASURED mastery — labeled facts, not recommendations), misconception watch, recent activity, predicted grade = explicit not-implemented state (never a fabricated number); `KnowledgeGraphView` (F-036) — 2D-first SVG (layered columns by KG depth, band-colored nodes, solid PART_OF + dashed prerequisite edges with arrowheads, legend, keyboard-selectable nodes with aria-labels; tree view stays the accessible alternative); `MasteryMap` refactored onto ONE personalized payload — the client-side tree + /state join is retired (F-034's purpose); Tree/Graph toggle + graph node detail card + on-demand prerequisite chains + Practise CTAs; controlled tabs with dashboard → practice topic deep links (`api.questions(topicNodeId)` already existed; honest "no validated questions on this topic yet" empty state); `lib/format.ts` shared formatters (StateView deduped). Local `bun run lint` clean + `bunx next build` (type-checked) SUCCESS. **PR CI run 34051246049 SUCCESS — step-level verified (Lint ✓, Production build type-checked ✓).**
  - Deliberately out of scope: 3D force-graph (F-036 is 2D-first by scope), pan/zoom, SSE, teacher surfaces (T-029 next), recommendation policy (the recommendation package stays empty), any change to GLM-OCR/T-C04 state, core PR #1 (T-026/T-027) untouched.

## Wave 4 — pilot hardening

- [x] T-029 Teacher minimal surface: class list, Smart Mark review queue, overrides. — **COMPLETE and MERGED 2026-09-07 (session 16, after the user-mandated pre-merge audit passed). The documented merge order was respected: core PR #7 FIRST, then web PR #3 — both merged as fast-forward pushes (linear history, no merge commit; each main is exactly the CI-verified head, GitHub marked both PRs closed/merged with merge_commit_sha = the pushed head). The audit added one test-only commit to the core branch BEFORE merging (real-HTTP JWT RBAC regression `TeacherRouteSecurityIT`); no product code changed by the audit.**
  - **Core PR #7 — MERGED** (fast-forward `ab7a52b` → `c7d22b9`; PR closed/merged, merge_commit_sha `c7d22b9`; branch CI 34054418641 on `095a9c1` SUCCESS (214/214 unit + 23/23 IT) and audit-fix head CI **34093970676** on `c7d22b9` SUCCESS — 240/240 tests: 217 unit (214 executed + 3 new security IT) + 23/23 IT; post-merge main CI **34094254832** SUCCESS, log-inspected: 0 failures/errors/skipped, TeacherRouteSecurityIT 3/3, GlmOcrBatchIT 5/5, GlmOcrBridgeIT 7/7, MultipartMarkingFlowIT 2/2, KaRagFlowIT 3/3, BUILD SUCCESS): `GET /api/v1/teacher/learners` (`TeacherRosterController` + `UserRepository.findEnabledByRole` JPQL — the honest class list IS the enabled STUDENT cohort; the pilot has no class entity; identity projection only: id/displayName/email/createdAt, test-pinned to exactly 4 record components so password hashes/roles cannot leak); `learnerDisplayName` composed SERVER-SIDE into `AnswerMarkingView` (one batched `findAllById` per queue read; empty queue skips the lookup — the F-034 lesson: server composes the teacher read model, no client-side join). The marking/override/κ backend itself already existed on main (T-021/T-022). **Authorization (audit section 4, the mandatory proof): backend route rule `SecurityConfig` `.requestMatchers("/api/v1/teacher/**").hasAnyRole("TEACHER","ADMIN")` — NOT frontend hiding; now regression-proven by `TeacherRouteSecurityIT` (real HTTP + real JWTs over the embedded server: a STUDENT token is proven VALID on its own `/api/v1/learners/me/state` surface then denied 403 on ALL seven teacher endpoints incl. roster/queue/detail/smart-mark/human-mark/κ-latest/κ-evaluate; anonymous and forged tokens 401 with no email leakage in the body; TEACHER and ADMIN 200).** One CI iteration pre-audit, a real find: the new IT ingested the same draft as the sibling test → duplicate `uq_knowledge_node_code` on the ingestion anchor → drafts now take distinct paper codes per test.
  - **Web PR #3 — MERGED** (fast-forward `18543d2` → `2c404a4`, after core PR #7 per the runtime dependency; PR closed/merged, merge_commit_sha `2c404a4`; post-merge main CI **34094459463** SUCCESS — Lint ✓, Production build type-checked ✓, log-inspected "Compiled successfully" + "Running TypeScript"): role-gated Teacher tab (TEACHER/ADMIN — a UI affordance, never authorization; the backend route rule above is the boundary); `TeacherReviewView` — class list (cohort table with honest "classes are a Cycle-2+ concept" framing), marking queue with state tabs mapping exactly the backend `Answer.MarkingState` values (PENDING/SMART_MARKED/HUMAN_MARKED/OVERRIDDEN), answer review panel (part prompt, learner answer, latest Smart Mark with failure reasons rendered verbatim — never fabricated — and model/pipeline traceability, latest human mark), **Run Smart Mark** action (single-flight, refreshes detail+queue+κ), **Record human mark (override)** form (0..partMarks integer validation client-side, backend re-validates via ConflictException; per-point decisions seeded from the smart breakdown for κ pairing, rationale comments; first-mark-fires-evidence-once / overrides-never-re-fire semantics in the UI copy), κ gate panel (latest evaluation κ/sample/threshold/passed/scope/age + recompute + honest 404 "no evaluation recorded yet" / 409 "no paired decisions available yet" states — fail-closed framing). Local `bun run lint` clean + `bunx next build` (type-checked) SUCCESS (audit-rerun on the branch head). API integration audited: all 7 client methods match backend paths/DTOs exactly, no hardcoded learner or marking IDs anywhere.
  - Deliberately out of scope: class management (no class entity in Cycle 1), teacher analytics/heatmaps (F-074 is Cycle 2+), content-validation workflow UI (T-C04 acceptance criteria unchanged — this surface must not be declared to satisfy T-C04), per-learner diagnostics drill-downs (§6.10 deeper items), SSE. Note: this UI may eventually provide an application-native surface for portions of the T-C04 human validation workflow, but that does NOT redefine T-C04, and T-029's UI must not be declared to satisfy T-C04 (validation remains a human gate with its own evidence package).
  - Audit findings (session 16, classified per the audit protocol): **A (blockers): none** — the authorization boundary was already enforced by SecurityConfig; the missing executable proof was closed pre-merge by `TeacherRouteSecurityIT` (test-only commit, CI-verified before merge). **B (non-blocking, documented):** web `loadKappa` renders non-404/409 errors (network/5xx) as the "No κ evaluation recorded yet" empty state — the Evaluate κ button still surfaces the real error; cosmetic honesty nit, not fixed to avoid scope creep. Stale `tab` value edge case if a session's role changed mid-login. **C (future work, NOT in this PR):** κ panel per-paper scoping UI (backend supports PAPER scope; the UI calls ALL scope only — tracked in the F-161 note); marking-queue pagination/ordering beyond pilot scale; roster → learner drill-down (§6.10).
- [ ] T-030 Research export (anonymized) + experiment registry v0. (F-146, F-154)
- [ ] T-031 Render deployment (Docker, keep-alive, cold-start UX) + Vercel production env. (F-001, F-156) — **configs + runbook MERGED 2026-09-08 (core PR #10, branch `codex/t-031-pilot-deploy-configs`): `render.yaml` Blueprint (Docker, free plan, /actuator/health check, all secrets sync:false — Neon JDBC, JWT, CORS origins, LLM keys, R2) + `docs/DEPLOYMENT.md` pilot runbook (provision order, account policy incl. the one-time teacher provisioning path, the pre-pilot verification checklist = T-032 gate inputs, cold-start/decay/backup notes). The actual Render/Vercel deployment itself remains a manual operator step (credentials outside the repo) — this task stays open until the deployed URL passes the runbook checklist.** Session-20/21 note: the runbook's Vercel env-var instruction was corrected in core PR #11 (MERGED 2026-09-08; the old `.../api/v1` value doubled the prefix — browser-verified locally); the full student+teacher browser E2E has been executed once against a LOCAL live stack (evidence in `download/e2e-evidence/`), so the runbook checklist is now proven executable — re-run it against the DEPLOYED URLs after provisioning.
- [ ] T-032 Cycle-1 pilot readiness review against Master Spec §39a exit criteria.

## Wave 5 — pilot hardening (session 20, ship/verify/harden mode)

- [x] T-035 Merge the browser-E2E hardening PRs (HUMAN decision): core PR #11 + web PR #6 (`codex/pilot-e2e-hardening`) — **MERGED 2026-09-08 on explicit user authorization (session 21): pre-merge review (diffs read line-by-line, scope verified = exactly the four browser-found bugs, head-branch CI 34207204653/34207223589 SUCCESS), fast-forward merges preserving linear history — core `14cc866→c4af544`, web `99194af→e4a06cb` — both PRs closed+merged (GitHub API verified), post-merge CI log-inspected: core run 34228311619 on main @ `c4af544` = 270/270 unit + 30/30 IT across 10 IT suites incl. MisconceptionSurfaceFlowIT, BUILD SUCCESS; web run 34228321719 on main @ `e4a06cb` = lint + type-checked production build SUCCESS.** Fixes: MISCONCEPTION_OF read direction (misconception rule family + map annotations + trees were dead on every read surface), API base `/api/v1` normalization, honest auth 401s, structured-history badge, decay-test tolerance.
- [ ] T-036 Provision the deployed environment (operator): Neon project + pgvector, Render blueprint deploy, Vercel project with the corrected env var, at least one free LLM key (Groq) for tutor/Smart Mark value paths, teacher account via the runbook's one-time provisioning path, then re-run the full browser E2E against the deployed URLs and record evidence. **Session-22 status (2026-09-08): deployment REHEARSED at production fidelity — Dockerfile-parity artifact built (JDK 25), prod-profile boot verified against external Postgres 17.11+pgvector (Flyway V1–V14 all-success, health UP in 16 s, R2-less + LLM-less boot proven post-fix), production web build + full browser E2E green (19/19 backend checks; adaptive loop re-proven with identical BKT/BDT numbers; isolation, teacher surface, honest error states; latencies measured). Two rehearsal-found bugs fixed + PR'd, CI-green, MERGE-PENDING: core PR #12 (R2-less boot failure — render.yaml hardcodes type=r2 while creds are optional) and web PR #7 (local-only demo credentials rendered on the production login screen). The workspace holds no cloud credentials (verified — none in env, no CLI tools, no config dirs): the cloud deploy itself remains REQUIRES OPERATOR ACTION — operator package with the exact sequence, safe-vs-secret env checklist, post-deploy verification commands and pilot failure procedures delivered at `download/t036/OPERATOR_DEPLOYMENT_PACKAGE.md`. Next: merge PRs #12 + #7 → execute the package → re-run the runbook checklist against the deployed URLs.** **Session-24 status (2026-09-10): BACKEND HALF DONE AND VERIFIED — Render service LIVE at `https://syllabai-core.onrender.com` (deploy 4, main `b35aa62`, CI green; boot 135.6 s, Flyway V1–V14, all three LLM providers registered — operator configured Groq + Gemini + OpenRouter keys; R2-less boot WARN as designed). Getting there fixed three distinct production-only bugs (deploy 1: nonexistent Maven base-image tag → `9d18303`; deploy 2: Groq client construction failed only when a real key is present — a path CI's zero-key boots never cover → `91441c0`; deploy 3: JVM footprint maxima ~850 MB vs the 512 MB free-instance cgroup → silent GC-thrash stall mid-Hibernate → port-scan timeout → small-container JVM recipe in the Dockerfile → `b35aa62`; full triage at `download/t036/RENDER_FAILED_DEPLOY_TRIAGE.md`). API verification 12/12 GREEN against the live URL (`scripts/t036_verify_deploy.py`): health UP, register/login/me, 8 seeded MCQs with no answer-key leakage, wrong answer → MIS-T1.1-01 misconception evidence, correct answer marked, attempt history read model, NBA 4 actions off the weak-mastery signal, teacher 403 / admin 403 / anonymous 401, tutor honest deterministic-refusal (no content ingested yet). CORS probed live: the allow-list is currently the DEFAULT (`http://localhost:3000,https://syllabai.vercel.app`) — the Vercel project must be named `syllabai` to match, or `SYLLABAI_CORS_ORIGINS` must be set in Render. REMAINING for T-036: (1) Vercel import + `NEXT_PUBLIC_API_BASE_URL` (operator, guided this session); (2) teacher account via the runbook one-time path; (3) full browser E2E re-run against the deployed URLs with recorded evidence.** **Session-24 follow-up (later 2026-09-10, operator pasted the live URL): Vercel half DONE — `https://syllabai-web.vercel.app` is LIVE (HTTP 200, Vercel edge, Next.js prerender, "SyllabAI — Learner Workbench"); the operator named the project `syllabai-web` (not the zero-CORS-change name `syllabai`) and set the env var correctly — `NEXT_PUBLIC_API_BASE_URL=https://syllabai-core.onrender.com` is verified BAKED INTO the deployed production bundle (direct JS-chunk inspection: `e3="https://syllabai-core.onrender.com".replace(/\/$/,"")`; the sandbox `XTransformPort=8080` fallback branch is present-but-dead). With that project name the CORS default does NOT match, and the preflight from the real origin was re-verified as an ACTUAL 403 (no ACAO) — control tests passed (`localhost:3000` → 200 + ACAO echo; `syllabai.vercel.app` → 200; no-origin → 401 auth challenge = app healthy), isolating the break to exactly one setting. THE CORS BLOCKER IS FIXED (2026-09-10, later still): operator set `SYLLABAI_CORS_ORIGINS` on Render; preflight from the real origin now returns 200 + `access-control-allow-origin: https://syllabai-web.vercel.app`, and a full browser-mode verification (Origin-attached preflight + register + login + 8 MCQs + wrong-answer attempt → MIS-T1.1-01 evidence + 403/401 boundaries) passed 11/11 (`scripts/t036_browser_mode_verify.py`). REMAINING for T-036: (2) teacher account via the runbook one-time path; (3) the human browser E2E pass over the six tabs with recorded evidence — the API-level path is now fully proven.**
- [x] T-036a (operator merge decision) Merge the two rehearsal-found fix PRs: core **#12** (`codex/t-036-r2-boot-fix` — lazy S3Client, boot WARN, loud per-op failure, 3 regression tests, render.yaml comment + DEPLOYMENT.md R2 section; CI 34243546661 SUCCESS) and web **#7** (`codex/t-036-login-demo-creds-prod` — NODE_ENV-gated demo-credentials box; CI 34244900527 SUCCESS). **MERGED 2026-09-09 on explicit user authorization ("Merge.") in session 23: core fast-forward c4af544→8e1f093, web fast-forward e4a06cb→376f4c5; 18/18 pre-merge checks (open/mergeable/head-sha/base-ancestor/CI-green/commits=1/main-unmoved); post-merge CI log-verified green (core run 34263167841, web run 34263199468).**
- [ ] T-037 Pilot data seeding decision: keep the validated V7 8-MCQ bank + teacher-validated structured content as the Cycle-1 practice surface; quarantine GLM-OCR corpus per T-C04; ~50 retake students self-register (STUDENT role by design); demo/test data stays `local`-profile-only and clearly separated.

## Content-ops track (parallel)

- [x] T-C01 GLM-OCR parser pipeline: real-corpus Markdown adapter + QP/MS extraction + mark reconciliation + image-reality handling + deterministic canonical identity + Python/Java conformance. (Session 8/9 — syllabai-parser @ `9eb35ab`, 68/68 tests + 12/12 conformance in CI; validation report `docs/validation/session9-real-corpus-validation.md`)
- [x] T-C02 GLM-OCR → core ingestion bridge: parser verified drafts → existing T-011 ingestion path → canonical persistence → T-013 chunk pipeline. — **COMPLETE and merged: PR #3 merged 2026-09-05 (fast-forward, no merge commit — main is now exactly `7c6f122`, the new content baseline)**. History: implemented on branch `codex/t-c02-glmocr-bridge` (CI run 33912946878: 185/185 unit + 16/16 IT incl. GlmOcrBridgeIT 6/6 on the 3 real WPH11 pairs; V13 bridge record keeps the verbatim parser contract — October Q18 + 1A 80-vs-120 conflicts review-visible; rerun-idempotent; embedding structurally separate; controlled entries = teacher endpoint + ops CLI; `docs/t-c02-bridge.md`); pre-merge correctness fix `7c6f122` "distinguish empty glm-ocr findings from missing bridge record" (missing bridge record → 404; existing record with empty `review_findings` → 200 `[]`) — 188/188 unit + CI run 33981175174 SUCCESS on the branch. **Post-merge verification on main @ `7c6f122`: CI run 33982065341 (push, main) — 188/188 unit + 17/17 IT (GlmOcrBridgeIT 7/7), BUILD SUCCESS; local `mvn test` 188/188 + `mvn verify` BUILD SUCCESS (ITs skipped locally, no Docker — as designed).** No new architecture; no embedding changes.
- [x] T-C03 ONE controlled Past-Papers batch end-to-end + human review, before any wider corpus run (the 3-pair proof is done; 40 batches stay parked until T-C03 review). Scope locked 2026-09-05: **a single bounded batch of real GLM-OCR QP/MS pairs through GLM-OCR → canonical documents → T-013 chunks → T-011 paper/question/mark-scheme model → bridge audit**; all imported content stays SUGGESTED; deterministic reruns (zero new rows); conflict preservation (never merged away); **no automatic embedding and no learner-facing serving**; batch audit report for human review before any wider run. Not a corpus-wide firehose. — **COMPLETE and merged 2026-09-06: parser PR #1 merged (main `9eb35ab` → `ba36ceb`, fast-forward; post-merge CI run 34015403577 — 71/71 + FULL conformance) + core PR #5 merged (main `7c6f122` → `1976540`, fast-forward; post-merge CI run 34015454011 — 196/196 unit + 22/22 IT incl. GlmOcrBatchIT 5/5, BUILD SUCCESS)**. History: implemented on branch `codex/t-c03-batch` in both repos (core PR #5 @ `4b820cc` CI 33983936464: 196/196 + 20/20 IT; parser PR #1 @ `ba36ceb` CI 33983496464: 71/71 + conformance). **Pre-merge review (session 12) found the product code sound but the two critical claims unproven by tests — added two regression ITs at `1976540` (branch CI 34015269882 green): (1) whole-batch atomicity — a late-failing pair (re-identified 1A copy whose canonical docs ingest fresh, then T-011 refuses the duplicate paper) rolls back the ENTIRE batch to pristine seed; (2) the real learner-serving boundary — QuestionController selection excludes all 59 imported ids while the 8 seed MCQs still serve, anchor-topic query empty, imported fetch 404, authoritative fetch serves.** Batch evidence (audit artifact, CI-verified): +6 canonical documents / +91 chunks (91/91 pending — no implicit embedding) / +3 papers / +59 versions / +59 schemes / +173 points / +3 bridge records, all SUGGESTED, second run zero new rows; June 20q/20p/51mp OK (80/80) findings 25; October 20/26/53 OK (80/null) findings 27 (Q18 visible); 1A 19/25/69 REVIEW_REQUIRED (80-vs-120 preserved) findings 32; all five invariants PASS; audit artifact downloadable from CI + workspace `download/t-c03-batch-audit-report.json`. **Next gate: T-C03 human content validation (see below) — only then any wider batch.** No new migration; no new HTTP endpoint; bridge untouched; PR #1 (T-026/T-027) untouched.
- [x] T-C05 Pre-Ingestion Standard + content-corpus architecture for the operator's manually prepared **Edexcel International GCSE Chemistry (4CH1)** corpus (specification / Student Book / past papers / Save My Exams notes & questions — operator converts manually with ocr.z.ai; SyllabAI does NOT convert and does NOT touch Cycle 1 IAL scope). Delivered as a contract-only design, no code: source-role model (5 roles, never flattened), **Corpus Markdown Convention (CMC v1.0)** for the manual conversion (front-matter schemas, heading grammar, chemistry notation rules, marks/MS conventions, assets & figure-missing markers), canonical metadata contract, chemistry-specific OCR lint vs human-review split, pipeline-stages × six-validation-axes lifecycle on the existing SUGGESTED→VALIDATED gate, four-tier spec-point mapping provenance (PUBLISHER/PROVIDER · AI_SUGGESTED · RULE_DERIVED · HUMAN_VALIDATED), evidence-based difficulty states (UNEVIDENCED→PROVISIONAL→EMERGING→EVIDENCED, per-CurriculumVersion population), provenance/versioning chain, five-level dedup semantics, `syllabai-corpus` repo layout, system-gap audit (SPEC_POINT node type + F-168 mapping provenance + `documents.kind`/`questions.provenance` enum extensions registered), and the **IAL↔IGCSE coexistence audit** (found: `ContentRetrievalService`/`ContentVectorRetriever` have NO curriculum scoping — registered as T-C07, a hard prerequisite before corpus embeddings are enabled; KG node codes must be namespaced `4CH1-*` because `knowledge_nodes.code` is globally unique). Doc: `CONTENT_CORPUS_ARCHITECTURE.md`. (Session 25, 2026-09-10) **Review pass on the first uploaded batch (same day, session 26): `syllabai-resources` reviewed and repaired (repo `8b9b5c3` → `c2b0e69`) — 5 expiring ocr.z.ai spec images rescued to local `assets/` (URLs died 2026-09-17), CMC front matter added to the spec md (`status: raw_ocr`), Web-Clipper `{pageTitle}` image bug repaired across all 112 SME notes (244/247 refs resolve; 3 figure-missing markers; re-runnable `scripts/c05_repair_clipper_refs.py` shipped in the repo), quality verdict: spec-point backbone intact (all sections + C-points + 12 practicals + Appendix 5 command words), chemistry notation degraded exactly as CMC predicted (CJK leak, lost sub/superscripts, full-width punctuation) — pipeline lint owns the fixes, not hand-editing; Student Book: convert the 383-page 1-up file, not the 192-page 2-up (page-level provenance). Findings + operator guidance: ` Same-session addendum: **Student Book OCR tooling decided (side task) — baidu/Unlimited-OCR (MIT, 25k stars) replaces ocr.z.ai for the 383-page image-only book.** Pilot verdict STRONG YES on 5 chemistry-stress pages via the HF demo Space (ion charges, subscripts, and full equations with state symbols preserved as LaTeX — vs ocr.z.ai's flattened CO32- on identical content); shipped c05_book_ocr.py (resumable per-page conversion driver, CMC front matter + 29-chapter map, two backends: official vLLM docker image for the full run on a rented GPU, HF Space for pilots — anonymous ZeroGPU quota empirically exhausts after ~8 calls); evidence + execution paths in syllabai-resources/BOOK_OCR_RUNBOOK.md + student-book-pilot/. Residual damage class ((l)→()/(1), Ar→A,, figure placeholders) stays lint-owned; LaTeX→HTML sub/sup normalization registered as a deterministic T-C06 converter step. (repo 53656a9)**
- [ ] T-C06 (PARKED until a starter tranche of corpus files exists + T-C04 verdict) CMC→canonical converter + corpus lint: syllabai-parser adapter + core ingestion endpoint targeting canonical schema 1.0 + draft DTOs; `documents.kind` += TEXTBOOK/EXTERNAL_NOTES/EXTERNAL_QUESTIONS; `questions.provenance` += EXTERNAL_BANK; `SPEC_POINT` node type with namespaced codes (`4CH1-…`); F-168 mapping-provenance columns on question mapping; stimulus/figure attachment design. All corpus imports land SUGGESTED; nothing serves without human validation.
- [ ] T-C07 (REGISTERED from the T-C05 coexistence audit — pre-embedding prerequisite) Curriculum-scoped retrieval for the tutor evidence path: `documents`/`document_chunks` scope + vector query filter by the learner's active CurriculumVersion (index-time AND query-time); evidence source labels distinguishing spec/textbook/third-party providers. Required BEFORE any corpus embedding is enabled; today it is latent only because the GLM-OCR corpus is quarantined and unembedded.
- [ ] T-C08 (future) Evidence-based difficulty states wired into serving policies: per-(question part, CurriculumVersion population) statistics jobs (p̂, discrimination, response time, sample state); recommendation/mock weighting by evidence state; new-question exploration share.
- [x] T-C09 (KG build Phase 1) Deterministic specification-skeleton extraction from the 4CH1 Issue 3 spec md → graph-as-code in syllabai-resources. — **COMPLETE (session 28, 2026-09-11; repo `c09-phase1`): `scripts/c09_spec_graph_extract.py` (deterministic, zero-LLM, hard-gated) + `scripts/graph_check.py` (validator, 8 check groups, negative-tested against 8 corruption classes) + `graph/` = 6 YAML files (182 specification_points · 4 topics + 28 subtopics · 210 PART_OF edges · 25 command words · 12 practicals · 3 AOs + 2 papers) + 2 reports (`PHASE1_COMPLETENESS.md`, seeded 20-statement `SPOT_CHECK_SHEET.md`). Ground truth amended by the statement-level PDF reconciliation: **182 unique codes (S1:60/S2:50/S3:22/S4:50) / 52 C-points / 28 subsections** — supersedes the plan's §5 167/40/29 baseline (evidence in PHASE1_COMPLETENESS §2; the md holds all 182 but 4.49C/4.50C are OCR-orphans outside the HTML tables). Cross-check: 119 exact / 52 normalized / 11 fuzzy (residual OCR damage) / **0 mismatch**, statement order identical, md↔PDF code sets equal; damage policy honored (verbatim text, 54/182 flagged, never fixed; 4SD0 whitelisted only inside the Double-Award applicability rule). Two real defects found by the gated first run and fixed in the extractor: (1) PDF-walk listing pollution — the section-start sub-topic TOC listings glued onto the previous section's last statement (1.60C/2.50 mismatches + 3.22C hidden fuzzy false-pass) → listing-state machine; (2) table-classifier ordering — Appendix 1's 'Paper codes' row substring-matched `paper_overview` before `codes_appendix`, emitting a bogus third paper record → check reorder + `papers: 2` gate. Per-node provenance: issue + md line anchor + table/row index + row shape + PDF page + match level; edge vocabulary = live V2 enum (PART_OF only, names never invented); `4CH1-*` namespace enforced everywhere. DB wiring rides T-C06 (CurriculumDraftDto → SPEC_POINT, built in T-010). Operator gates: SPOT_CHECK_SHEET (20 statements) + git PR review = the HUMAN_VALIDATED promotion.**
- [x] T-C10 (KG build Phase 2 — **COMPLETE 2026-09-11, session 29; spot-check sheet operator-reviewed 2026-09-11: 19/20 confirmed (one after machine visual verification of the lattice diagram) + 1 REJECTED (4CH1-4.15) remapped to the Nitrogen Oxides & Sulfur Dioxide note (see scripts/c10_rework_415.py + the sheet review record); remaining gate: the git PR front-matter review (HUMAN_VALIDATED promotion)**: all 112 SME notes carry a `spec_map:` front-matter block = deterministic PROVIDER subsection anchor (source-URL slug; 28/28 groups align 1:1 with the spec subsections, ordering exact, verified programmatically) + **211 AI_SUGGESTED point-level mappings** (high 176 / medium 34 / low 1), each with a verbatim evidence quote + confidence + model version + rationale + `validation_status: SUGGESTED`. Hard gates ALL GREEN: evidence-verbatim-in-note anti-hallucination gate (caught 2 quote defects during the build); every code ∈ the 182-point registry; every note ≥1 mapping; note bodies byte-identical (+3242 front-matter lines / 0 deletions); idempotent apply. Coverage: **182/182 points have ≥1 direct mapping — the Phase-3/4 zero-coverage queue is EMPTY** (quality gaps still flagged, e.g. poly(tetrafluoroethene) missing from the polymer note's examples). Validator: `graph_check.py` group 6 `c10-notes-mapping` (9 groups ALL PASS) + 8 new corruption classes negative-tested. Reports: `graph/reports/PHASE2_MAPPING_COVERAGE.md` (method, per-subsection + per-point detail, 1 cross-subsection flag 4CH1-1.17, PR review guide) + `PHASE2_SPOT_CHECK_SHEET.md` (20 sampled mappings). Tools: `scripts/c10_worksheets.py`, `scripts/c10_map_notes.py`, `scripts/c10_decisions/S*.json` (the AI decision record), `scripts/c10_negative_test.py`. DB wiring still rides T-C06; zero core changes. Semantics per `KNOWLEDGE_GRAPH_CONTEXT.md` §8A.7: PROVIDER slug signal ranked above semantic similarity, uncertainty preserved per mapping. **PR-review tooling (2026-09-11, session 31): the remaining operator gate is operationalised** as `graph/reports/PHASE2_PR_REVIEW_GUIDE.md` (deterministic work order in the operator's priority sequence: remapped 4.15 → the 1 low → the 34 medium with cross-note-deferral flags first → the 1.17 cross-subsection flag → the deferral/semantic-completeness scan → the diagram-dependent queue incl. the 3 figure-missing notes); **promotion pathway is decisions-side** (per-mapping `validation` blocks in `scripts/c10_decisions/S*.json`, gated by applier G7, carried into front matter as HUMAN_VALIDATED + validated_by + validated_date — hand-editing note front matter would be silently reverted by the next applier re-run); `scripts/c10_promote.py` batches promote + gated re-apply; validator c10.3 accepts exactly SUGGESTED or a complete HUMAN_VALIDATED block (negative-tested 10 classes + positive control); promotion rehearsal executed end-to-end then fully reverted — on-disk state is 0 promoted / 211 SUGGESTED, awaiting the operator's PR review) **; PR-review EXECUTED 2026-09-11 (session 32, AI review pass under the operator's clarified mapping contract): `graph/reports/PHASE2_PR_REVIEW_SHEET.md` records 61 distinct mappings reviewed (4.15, 1.4, all 34 mediums, both 1.17s, the 21-mapping diagram queue with VLM verdicts archived in `scripts/c10_vlm_results/`, the 7 missing-figure mappings) — 61 CONFIRM / 0 REJECT / 0 HOLD under the §0.0 contributory many-to-many contract (SME notes group and split spec points; 69/112 notes carry 2+ points, 25/182 points covered by 2-3 notes); 6 rationales reworked for honest contributory wording (4.15, 1.4, 1.16@Atoms, 2.29, 3.10, 1.17@S1-e) with evidence/confidence/tier/validation unchanged; all gates re-run green, note bodies byte-identical; the one remaining step to close T-C10 is the operator's ratification command (staged, validated 61-spec batch, sheet §11) — until then on-disk state stays 0 promoted / 211 SUGGESTED. **Ratification audit (2026-09-11, session 33): the operator-advisor's pre-execution condition is DISCHARGED — `scripts/c10_ratify_audit.py` (read-only, resolves the §11 specs with the PRODUCTION `c10_promote.py` resolver) proves the batch promotes exactly the 61 reviewed CONFIRM pairs: bijection 61/61 both directions, 30 bare + 31 @FRAGMENT specs all unambiguous (zero code-wide promotion, zero unreviewed targets), pre-state 0 promoted / 211 SUGGESTED; 10/10 checks PASS, report `graph/reports/C10_RATIFICATION_AUDIT.{json,md}` — the §11 batch is machine-verified ready to run**** **; ROUND-4 REWORK (2026-09-11, session 34): the advisor's fourth review round rejected 2 of the 61 confirms — 4CH1-4.15 (explain-how point: no corpus note teaches the impurity→SO₂ formation explanation; the round-3 CONFIRM had aggregated premise + stated result = inferred coverage) and 4CH1-1.17 @ Calculate Relative Mass (calculate point: definitional derivation-basis sentence only, duplicated in the S1-c note) — both removed via `scripts/c10_round4_rejects.py` (idempotent): store 211→209 (176-32-1, 68/112 multi-point), coverage 182→181 with 4.15 an ANNOTATED zero-coverage corpus gap (T-C11 input: pieces exist, connective teaching does not), the command-kind substance rule frozen as guide §8, review sheet §12 superseding §1/§11 with the staged 59-spec batch (§11 marked ON HOLD), all gates re-run green (applier 209, graph_check 9/9 at 112/209/181, negative test 10+PC); the re-targeted ratification audit passes 12/12 (C1b/C4c new: both REJECTs proven removed from the store and absent from the command; bijection 59/59) — the ONE remaining step: the operator runs the sheet §12 command → 59 HUMAN_VALIDATED, 150 stay SUGGESTED risk-tier, 4.15 stays an honest corpus gap**** **Round-5 addendum (2026-09-11, session 35): the remaining 150 mappings are now formally AI-reviewed — 150 CONFIRM / 0 REJECT / 0 HOLD under the frozen contract (full-note reads, 8 VLM diagram checks, 18 adversarial flags, 5 corpus-level partial-coverage observations for T-C11; artifacts `C10_ROUND5_REVIEW.json` + `PHASE2_ROUND5_REVIEW_SHEET.md`; store untouched at 209/0-promoted/all-SUGGESTED; resources c6454c9 → c90f5ae). Operator decisions pending: ratify §12's 59-spec batch; ratify the 150 (single controlled promotion or risk/section split).****** **Session-37 addendum (2026-09-11): T-C10 CLOSED — operator ratification executed ("Ratify the 59 and promote 150"): the §12 59-spec command + the §13 150-spec staged batch (derived mechanically from the round-5 review JSON; dual-batch audit pre 17/17 PASS) both executed verbatim → **209/209 mappings HUMAN_VALIDATED** (validated_by `operator`, 2026-09-11; review trail rounds 3–5); post-execution audit 7/7 PASS (promoted == staged union exactly; all 150 evidence quotes retained byte-for-byte; the 2 rationale corrections applied verbatim — applied before promotion in resources commit b0008d4; all 112 note bodies byte-identical to the round-5 review state c90f5ae); gates green (applier `promoted HUMAN_VALIDATED: 209`, graph_check 9/9, negative test 10 classes + positive control with state-agnostic promotion corruptions); resources b0008d4 → 59b8207 (rebased over the operator's PMT mark-schemes push). T-C11 may begin; 4.15 stays the honest annotated zero-coverage corpus gap.** 
- [ ] T-C11 (KG build Phase 3 — after T-C10) Concept/prerequisite/misconception graph: canonical CONCEPT nodes + alias tables under a split-first identity policy (over-splitting is recoverable, over-merging corrupts mastery attribution; merges are human-only); PREREQ_OF edges enter as AI_SUGGESTED with evidence anchors and serve only after HUMAN_VALIDATED promotion (recursive-CTE closure already live in core); MISCONCEPTION nodes require source quotes (SME text / question distractors), and the triple distinction `COMMONLY_CONFUSED_WITH` ≠ `MISCONCEPTION_OF` ≠ `WRONG_ANSWER_PATTERN` is modeled separately (§8A.11); Student Book page-level `EXPLAINED_BY` enrichment after the full 383-page OCR run (chapter map already in the driver's CMC front matter). Gates: no orphan concepts; every edge provenance + evidence anchored; 100% of misconceptions source-quoted. Phase 4 (assessment graph) stays T-C06's F-168 mapping territory — corpus-blocked on past papers, seeded by the 11 SME topic-question pages; the 4CH0 fixture is hard-separated (no 4CH0→4CH1 mapping, checked loudly). **PILOT EXECUTED 2026-09-11 (session 38, per the operator's design-first tasking): architecture + pilot spec authored BEFORE any generation — `graph/reports/C11_ARCHITECTURE.md` (schemas/provenance/confidence-with-derivation-caps/validation states incl. REVIEW_REQUIRED/split-first identity/command-kind tagging/deterministic contract/4.15 negative-control rules/negative-test spec/V2 projection/§16 expansion criteria; §0 records the tasking audit — baseline verified, every divergence an omission resolved toward the frozen contracts, κ replaced by honest raw agreement). The 1.25–1.36 pilot graph: `scripts/c11_pilot_decisions.yaml` (AI decision record) → `c11_concept_pilot.py` (fail-closed, 12 gate families, byte-verified quotes, T-C10-mapping-resolved attachments, 4.15 structurally uncoverable, anti-forgery) → `graph/concepts.yaml` + `graph/concept_edges.yaml` + `graph/spec_command_kinds.yaml` = 29 nodes (27 CONCEPT + 2 MISCONCEPTION incl. 1 WRONG_ANSWER_PATTERN) / 66 edges (33 PART_OF + 26 REQUIRES_PREREQUISITE + 3 EXPLAINED_BY + 1 MISCONCEPTION_OF + 2 REMEDIATED_BY + 1 WRONG_ANSWER_PATTERN; 0 RELATED_TO / 0 COMMONLY_CONFUSED_WITH = honest abstention) / 64 SUGGESTED + 2 REVIEW_REQUIRED / **0 promoted**; 12 held/rejected candidates with named failing rules (incl. the 4.15 premise+consequence class REJECTED). Gates ALL GREEN: graph_check 11/11 (new c11 groups), `c11_negative_test.py` 14/14 (12 corruption classes + PC + idempotence; the 4.15 classes prove the negative control), deterministic regeneration byte-identical. Adversarial pass-2 + raw agreement (NOT κ): 29/29 nodes, 31/31 asserted edges confirmed, 2 uncertain edges concordantly non-asserted → operator decision; 3 FP findings + 3 FN concerns recorded. Reports: `C11_PILOT_REVIEW_SHEET.md` (THE OPERATOR GATE — per-row verdicts, settles the 2 REVIEW_REQUIRED edges, merges are operator-only) + `C11_PILOT_REVIEW.json` + `C11_PILOT_REPORT.md` + `C11_COVERAGE_GAP_REPORT.md` (4.15 = zero manufactured coverage, machine-enforced; boundary concepts + MS-mining breadth the expansion inputs). NEXT: operator reviews the pilot sheet → then (a) promotion batch tooling (c10_promote pattern) for the confirmed set, and/or (b) §16 authorization + scoped expansion plan for the full graph. No mass generation, no DB writes, no promotion until then.** ** REVIEW ROUND EXECUTED 2026-09-11 (session 40, operator review tasking): the two REVIEW_REQUIRED edges reviewed to the 10-field depth with byte-verified evidence and both verdicts CONCORDANT with pass-2 — `PR-03 → CON-MOLE` **REJECT** (implicit table-row label only + transitively subsumed via EXP-FORMULA-DEDUCTION) and `GAS-VOL-CALC → AVOGADRO-LAW` **HOLD** (worked example never names the law; alternative MOLAR-RATIO identity unauthored) — neither silently resolved, neither promoted. The 12 holds audited (11 CONFIRM_HOLD + 1 CONFIRM_REJECT, 0 reopened) and clustered into 4 stable failure classes → C11_ARCHITECTURE §19 (FC-1..FC-4; the RR edges instantiate the same classes). All 66 edges re-audited against the 7 semantics rules — every rule upheld, 2 medium-confidence judgments flagged review-gated (FP-1 + the CONSERVATION-MASS remediation mapping). Negative control machine-verified + 3 new variant lures rejected (`c11_task4_variants.py` 3/3). Promotion mechanism built & tested & INERT: `c11_promote.py` + `c11_promotions.yaml` (0 entries) + G13 + c11.10/c11.13 + `c11_promote_test.py` 25/25 (sandbox; exact identity, fail-closed, idempotent, anti-forgery, byte-preserving). `C11_S16_GATE_REPORT.md`: §16 NOT ready (operator blockers: RR settlement, sheet verdicts, first ratification + promotion + audit, expansion plan). Final state 29/66/12 byte-identical, 0 HUMAN_VALIDATED, all gates green; resources e218259 → 4eba8ea. Full record: `C11_PILOT_REVIEW_RESPONSE.md/.json`.**
- [ ] T-C12 (**RATIFIED — NEXT SESSION; owner decisions D1–D5 recorded 2026-09-11**, plan §8) PMT bulk-download backfill of `syllabai-pastpapers` per its `PMT-BULK-DOWNLOAD-PLAN.md`: charter §13 20-step workflow at bulk scale (download ledger → throttled fetch → PDF verify + identity spot-check → normalize §24 → per-subject batch commits via git CLI); priorities P0 complete IGCSE Chemistry 4CH1 series (2024-01, 2025-01, 2025-06, 2019-01?, missing (R)/(A) variants — ADR-019 pilot subject) → P1 IAL Chemistry WCH01–06 + WCH11–16 (zero coverage today) → P2–P5 remaining IGCSE/IAL series sweeps, biology, optional provenance backfill of the 2,512 `source_url: null` files; **binding decisions: D1 repo stays public (standing takedown policy) · D2 QP/MS only — ER sweep deferred · D3 Oct/Nov IAL sessions fetched wherever PMT lists them · D4 strict sequencing — P0 must CLOSE before P1 opens (Gate B, no interleave) · D5 ratification first — no PDF fetch until the operator's human-validation pass over the 1,361 `AI-IDENTIFIED` baseline dirs or a written waiver (Gate A, plan §8.1)**; politeness throttle + UA, `_staging/` never committed, PMT recorded honestly as third-party archive (charter §13 tier 5) with per-file page-of-record URLs; corpus-only work — no `syllabai-core`/platform ingestion (T-C06/T-C07 gates still apply), no learner serving from the repo.
